# Tiara 論文技術總覽（Primer）

> **這份文件是 `main.tex` 的技術伴讀。** 把論文裡出現的每一條公式、每一個演算法、
> 每一個名詞、以及被引用的每一篇論文，一次講完。
>
> **規則**：本檔只做「解釋」，**不新增任何主張，也不含任何論文裡沒有的數字**。
> 凡引用數字處皆標明來自 `main.tex` 的哪一節／哪一張表。論文標為 `NOT_MEASURED` 者，這裡也標。
>
> 對應版本：`main.tex`（2026-09-02 編譯版，14 條編號公式、2 個演算法、17 張表、6 張圖）
>
> **引用慣例**：文中所有 `§x.y`、`表 N`、`圖 N`、`式 (N)`、`演算法 N`、`附錄 A/B`
> **一律指 `main.tex` 的編號**（與 `main.pdf` 一致）。
> 指本文件自身時寫「本檔 §x」。

---

## 目錄

- [0. 三十秒版本](#0-三十秒版本)
- [1. 符號與名詞速查](#1-符號與名詞速查)
- [2. 背景技術：讀懂這篇論文需要的前置知識](#2-背景技術讀懂這篇論文需要的前置知識)
- [3. 四個觀察：動機章的量化事實](#3-四個觀察動機章的量化事實)
- [4. 全部 14 條公式，逐條解釋](#4-全部-14-條公式逐條解釋)
- [5. 兩個演算法，逐行解釋](#5-兩個演算法逐行解釋)
- [6. 設計：預測器與成本敏感損失](#6-設計預測器與成本敏感損失)
- [7. 量測結果：七項發現與 go/no-go](#7-量測結果七項發現與-gono-go)
- [8. 引用文獻核心：每一篇在講什麼](#8-引用文獻核心每一篇在講什麼)
- [9. 論文尚未做到的事](#9-論文尚未做到的事)
- [附錄 A：公式速查索引](#附錄-a公式速查索引)
- [附錄 B：所有實測常數一覽](#附錄-b所有實測常數一覽)

---

## 0. 三十秒版本

**問題**：長上下文 LLM 推論時，KV cache 隨上下文長度線性成長並吃光 GPU 記憶體。
既有工作分別處理了「卸載到 CPU/SSD」「壓縮」「驅逐後可召回」「重算 vs 傳輸」四件事，
但沒有人把它們合成**單一的線上決策**。

**出發點（一個量化事實）**：重算與傳輸的成本比 $\kappa$ **跨硬體變動達 32 倍**
——MI300X 上 6–21 倍、RTX 3090 上 54–190 倍（表 5）。
所以「一套 KV 放置策略適用所有硬體」這個隱含前提不成立。

**做法**：把 KV 放置寫成**受品質約束的序列決策問題**，動作空間六階：

```
GPU-BF16 → GPU-FP8 → GPU-INT4 → CPU → SSD → DROP（丟棄後重算）
   ↑ 精度分層（有損，不可逆）        ↑ 位置分層（無損，可逆）    ↑ 平時成本 0，被需要時最貴
```

在「品質退化 $\le \epsilon$」的硬性約束下，聯合最佳化**未來效用、傳輸成本、重算成本、記憶體壓力**四項。

**最關鍵的一句**：`DROP` 的價值不在單次成本（它最貴），而在**它閒置時不佔用任何資源**。
所以它的正確性 100% 取決於「未來效用預測」準不準——預測器不是附加元件，是這個動作成立與否的唯一依據。

**本稿狀態**：**線上策略尚未實作**。第 6 章是已完成的量測 + 離線 oracle 的 headroom，
判定為**條件式 GO**（權重須量化、請求長度須落在 $2$–$3.5\,P^{*}$）。

---

## 1. 符號與名詞速查

### 1.1 論文全文使用的符號（對應表 1 上半）

| 符號 | 意義 | 出現處 |
|---|---|---|
| $\mathcal{A}$ | 動作空間，式 (4) 的六個候選狀態 | §4.1 |
| $\kappa$ | **重算對傳輸的成本比** $t_{\text{recompute}}/t_{\text{transfer}}$ | §2.4、表 5 |
| $P^{*}$ | `SSD` 與 `DROP` 的**成本交叉位置**（單位：token） | 式 (13)、§6.8 |
| $\alpha$ | 重算成本對 block **絕對位置**的斜率（ms/token） | 式 (2)、表 12 |
| $\epsilon$ | 品質退化上限，約束 $\Delta\mathcal{Q}\le\epsilon$ | 式 (8)、§6.6 |
| $p_i$ | block $i$ 於決策視窗內**被再次存取的機率**（真值） | 式 (7) |
| $\hat{p}_i$ | 上式的**校準後估計值**（預測器輸出） | 演算法 1 |
| $p^{*}$ | **最佳決策門檻** $1/(1+\kappa)$ | 式 (9) |
| $\ell_i,\ \pi_i$ | block $i$ 的**所在層級**與**精度** | §4.1 |
| $\lambda_m,\ \lambda_x$ | 容量佔用與搬移成本的**權重** | 式 (7) |
| $\tau_i$ | block $i$ **距下次存取的時間**（預測器的迴歸目標） | §5.2 |
| $W$ | 決策視窗長度（relaxed-Belady 邊界） | §5.2 |
| $L,\ H_{kv},\ d_h,\ s$ | 層數、KV head 數、head 維度、每元素位元組數 | 式 (1) |
| $N_{\text{active}}$ | **啟用**參數量（MoE 只算啟用的專家） | 式 (6) |
| $C^{\text{gpu/cpu/ssd}}$ | 各階剩餘容量 | 式 (3) |

### 1.2 量測設定的代號（對應表 1 下半）

**模型剖面**（權重精度 + 成本常數的來源，三組常數不可互相替代）：

| 代號 | 模型與權重 | 角色 |
|---|---|---|
| `qwen-awq` | Qwen2.5-7B-Instruct-1M，AWQ-INT4 | **主設定**（原生 1M 上下文，24 GB 上的容量懸崖落在 ~315K，遠低於模型上限，故量到的是純記憶體限制） |
| `llama-awq` | Llama-3.1-8B-Instruct，AWQ-INT4 | 對照（$\kappa$ 減半） |
| `llama-bf16` | Llama-3.1-8B-Instruct，BF16 | 對照：隔離**權重精度**的影響 |

**Baseline**：`full_gpu`（不卸載，vLLM 預設）、`cpu_lru` / `cpu_arc`（vLLM `OffloadingConnector` 的
兩個內建策略，構成 production 預設的**完整**集合）、`tier_fs`（CPU + 磁碟兩階）、`lmcache`。

**KV 精度**（vLLM `--kv-cache-dtype` 的取值）：`auto`（BF16，無損基準）、
`fp8`（純格式轉換、靜態縮放）、`int8_per_tok_head`、`int4_per_tok_head`（per-token-head 動態縮放）。

### 1.3 縮寫與術語

| 術語 | 解釋 |
|---|---|
| **KV cache** | Transformer 自回歸生成時保存的每個已處理 token 的 key/value 向量，避免每步重算整段前綴。以記憶體換計算時間。 |
| **GQA** | Grouped-Query Attention。多個 query head 共用一組 KV head，$H_{kv} < H_q$，直接縮小式 (1) 的 KV footprint。 |
| **MLA** | Multi-head Latent Attention（DeepSeek 系）。KV 存的是**壓縮後的潛在向量**，改變式 (1) 本身。§6.9 量到 30.4 KiB/token，是 GQA 的 $1/4.2$。 |
| **MoE** | Mixture-of-Experts。KV 由 attention 層決定（與同規模 dense 相同），但重算只需**啟用的專家**，故 $\kappa$ 的分子小得多。 |
| **prefill** | 一次處理整段 prompt 的 $N$ 個 token。算術強度高，**受限於算力**。 |
| **decode** | 一次產生一個 token，但**每一步都要讀取整個 KV cache**。算術強度趨近 1，**受限於記憶體頻寬**。 |
| **PagedAttention** | vLLM 的 KV 分頁機制。block table **以 token 為索引且跨層共用**——這是「一個動作 $a_i$ 同時套用於 16 token 的全部 layer / 全部 KV head / K 與 V」的原因（圖 3a）。 |
| **chunked prefill** | 把 prefill 切成 chunk 逐段送入。這使重算 block $i$ 不必從 token 0 重跑，只需把 $b_i$ 的 token 前向通過模型，attention 讀前序既有的 KV → 成本 $O(C\cdot P)$ 而非 $O(P^2)$。 |
| **TTFT / TPOT** | Time To First Token（prefill 端到端延遲）／ Time Per Output Token（decode 每步延遲）。式 (8) 的兩條 SLO 約束。 |
| **HBM / GDDR6X** | 加速器上的高頻寬記憶體。MI300X 192 GB HBM3、RTX 3090 24 GB GDDR6X。 |
| **FLOP/byte** | **本文判斷「重算或傳輸」的正確指標**：dense BF16 峰值 ÷ 單向主機鏈路頻寬 = 一位元組穿越 PCIe 的時間內可 retire 的算術運算量。3090 = 2,254、MI300X = 20,752（表 4）。 |
| **MFU** | Model FLOPs Utilization。實際達成的 FLOPS ÷ 峰值。$\kappa$ 的估算假設 100% MFU，故表 5 的值為**下界**。 |
| **AWQ** | Activation-aware Weight Quantization，INT4 權重量化。**權重精度是正交的部署參數，不是本文的決策變數**——但它決定 KV 剩多少預算（§6.9）。 |
| **FP8 e4m3** | 1 符號 + 4 指數 + **3 尾數**位元。3 個尾數位元把相對精度鎖在約 12.5%，**縮放方式改不了**（§6.6）。 |
| **per-token-head 量化** | 每個 token × 每個 KV head 各一組 scale。動態、精度高，但**每 token 固定多帶 2 KiB 中繼資料**（§6.9）。 |
| **isotonic regression** | 保序迴歸。把預測器輸出的分數單調映射成**校準過的機率** $\hat p$。式 (9) 的門檻只在 $\hat p$ 校準過時才成立。 |
| **GBDT** | Gradient-Boosted Decision Trees（本文用 LightGBM）。原生處理缺失值與 $\infty$，CPU 上執行，訓練 300 ms、對 64 候選推論 30 μs。 |
| **EDC** | Exponentially Decayed Counters，LRB 的 10 個指數衰減計數器，功能等同循環網路的隱藏狀態，但更新規則寫死。 |
| **Bélády / MIN** | 1966 年的離線最優置換：逐出「下次使用最遠」者。**單階時可證明最優**，故 oracle 的 GPU 階用它。 |
| **ARC** | Adaptive Replacement Cache。vLLM 內建的另一個策略，在 recency 與 frequency 之間自適應。 |
| **headroom** | 離線 oracle 相對於**最佳可部署 baseline** 的改善幅度。**是上界不是成績。** |
| **sm_86 / gfx942** | NVIDIA compute capability 8.6（Ampere，RTX 3090）／ AMD MI300X 的架構代號。 |
| **DCA** | Dual Chunk Attention。Qwen2.5-1M 的長上下文外推機制；vLLM 0.28 的 V1 engine 無可用路徑，故評測用移除 DCA 的變體（§A.5）。 |
| **SDMA** | System DMA copy engine。ROCm 上 KV 搬移走這條路而非 AQL compute queue，故對 `rocprof-compute` 的 TCC/L2 counter **不可見**（§B.6）。 |

---

## 2. 背景技術：讀懂這篇論文需要的前置知識

### 2.1 KV cache 為什麼是問題

生成第 $n$ 個 token 時，attention 需要前面 $n-1$ 個 token 的 key 與 value。
不快取就要每步重算整段前綴（$O(n^2)$ 總計算）；快取則要把它們全部存起來（$O(n)$ 記憶體）。
標準做法是後者，代價是**記憶體佔用隨上下文長度線性成長**。

具體數字（論文 §1）：Llama-3.1-8B（32 層、8 個 KV head、$d_h=128$、BF16）
每 token 需 **128 KiB**，1M token 的上下文即需 **122 GB**。
在 80 GB 級加速器上，限制因素從**算力**轉移到**記憶體容量**。

### 2.2 位置分層 vs 精度分層：本文統一的兩個維度

既有工作**只做其中之一**：

| 維度 | 做什麼 | 有損？ | 可逆？ | 代表工作 |
|---|---|---|---|---|
| **位置分層** | GPU ↔ CPU ↔ SSD，搬位元組 | **無損**（§6.6 以 60/60 逐字元驗證） | **可逆** | ArkVale、Quest、ShadowKV、FlexGen |
| **精度分層** | BF16 → FP8 → INT4，丟位元 | **有損** | **不可逆**（丟掉的位元取不回來） | QEvict、KIVI、KVTuner |

**圖 3c 的核心觀察：階梯上沒有任何一條「精度往上」的箭頭。**
唯一能回到 `GPU-BF16` 的路徑是 `DROP` 之後重算。

這個不對稱有兩個後果：
1. $\epsilon$ 約束**只作用於精度那三階**，故 §6.2 的 oracle 在四個無損動作上求解時 $\epsilon$ 自動滿足；
2. 預測器的一次誤判**在位置維度上可廉價更正，在精度維度上則不行**。

### 2.3 prefill 與 decode 的經濟性差五個數量級

同一筆搬移，在 prefill **攤提於整段前綴**，在 decode **每產生一個 token 就重付一次**。

平台 A 實測（§2.5，Llama-3.1-8B 與 Qwen2.5-7B-1M，AWQ 權重）：

| 上下文 | prefill (ms/tok) | decode (ms/tok) | 倍數 |
|---|---|---|---|
| 16,384 | 0.316 | 17.0 | 54× |
| 65,536 | 0.591 | 23.5 | 40× |
| 126,976 | 0.903 | 29.7 | 33× |
| 258,048 | 1.264 | 29.4 | 23× |

以 126,976 上下文、FP8-KV 計（KV 總量 7.75 GiB，PCIe Gen3×16 = 15.75 GB/s → 全部搬到主機需 492 ms）：

| 卸載比例 | decode 每步增加 | 相對全駐留 |
|---|---|---|
| 5% | +24.6 ms | 3.0× |
| 20% | +98.4 ms | 11.9× |
| 100% | +492.1 ms | 59.3× |

**結論：在 full attention 下，decode 階段的卸載在任何比例都不可行。**
即使只卸載 5% 也要付全駐留 3 倍的代價。唯一可行路徑是**稀疏 attention**
（只取回當步 attention 分數高的 block），即 Quest / InfiniGen / ArkVale 的做法。

**這條界線決定了本文預測什麼**：$p_i$ 是**跨請求的時序量**（這段前綴會不會被下一個請求命中），
而不是請求之內的 attention 分數。在 full attention 的 decode 期間每個 block 每步都被讀取，
$p_i \equiv 1$，預測無從施力。

#### 那到底什麼時候才「搬」？——「分塊」與「能搬」是兩件事

> 這是最常卡住的一點：「KV 既然可以分塊，那不就可以隨時搬進搬出？」

**分塊是機制，能不能搬是時機。** 兩者要分開看：

- **KV 一直都是分塊的。** vLLM 的 PagedAttention 把 KV 切成 16 token 一塊，
  **prefill 和 decode 都一樣**。這跟階段無關。
- **但「能不能搬」取決於「這一步要讀多少 block」**，而 prefill 與 decode 在這一點上完全相反。

```
請求 r₁ 抵達
│
├─ [prefill] 一次處理整段 prompt 的 N 個 token
│     每個 block 寫入一次；搬移成本攤提於整段前綴
│
├─ [decode] 產生 token 1, 2, 3, … , M
│     ★ 每一步都要讀「全部」block ★
│     → 搬出去的 block，這一步要搬回來，下一步再搬一次… 共 M 次
│
├─ 請求 r₁ 結束
│
│   ┌────────────────────────────────────┐
│   │  ★ 這裡才是本文的決策點 ★          │
│   │  沒有任何人在讀這段 KV。           │
│   │  放 GPU / CPU / SSD / 丟掉，       │
│   │  都不影響任何正在跑的東西。        │
│   └────────────────────────────────────┘
│
└─ 請求 r₂ 抵達，前綴命中 r₁ 的 KV
      此時才付「搬回來」的成本，只付一次，攤在整段 prompt 上
```

**「移入移出」問題確實存在，但它發生在兩個完全不同的時間點**
（§2.5，126,976 ctx、FP8-KV、KV 共 7.75 GiB、PCIe Gen3×16 → 全部搬至主機需 **492 ms**）：

| 搬移發生在 | 付幾次 | 代價 |
|---|---|---|
| **decode 期間** | **每步一次，共 $M$ 次** | 每一步重付，而全駐留於 HBM 的同一次讀取僅需 **8.3 ms** → 卸載 **5%** 即付全駐留 **3.0×**，100% 為 **59.3×** |
| **請求之間** | **一次** | 同樣 492 ms，但**攤提於 126,976 個 token** |

**同一筆搬移，兩個階段的經濟性相差五個數量級。** 這就是為什麼：

> **在 full attention 下，decode 階段的卸載在任何比例都不可行。**

**這條界線決定了整篇論文在預測什麼**：
- **decode 期間**：每個 block 每步都被讀取 → $p_i \equiv 1$ → **預測無從施力，也沒有任何放置自由度**
- **請求之間**：「這段前綴會不會被下一個尚未抵達的請求命中」**真正未知** → **這才是 $p_i$ 的定義**

所以 §7.3 那句「decode 佔端到端時間 75%，而放置決策對其無自由度」不是抱怨，
而是把可作用的範圍講清楚：**放置策略只能作用在 prefill 與請求之間的空窗；
decode 那 75% 是分母，不是戰場。**

**兩個例外**：
1. **稀疏 attention 是 decode 唯一的出路**——只取回當步 attention 分數高的 block
   （Quest / InfiniGen / ArkVale）。但那預測的是 $q\cdot k$，是**不同的預測問題**，本文不處理（本檔 §8.7）。
2. **單請求 KV 超出 GPU 預算時，請求「之內」也必須逐出**（本檔 §7.6 的 524K 一列）。
   此時式 (5)(6) 的 `Drop` 依賴鏈就會咬人——被丟掉的 block 若位於前面，
   後面的 block 要重算時得**先把它重算回來**，成本沿依賴鏈累積。

> **⚠️ 一處算術待查（本檔核對時發現，不影響結論）**：
> 論文 §2.5 寫「492 ms 攤提於 126,976 個 token，等於每 token **0.0024 ms**」，
> 但 $492 / 126{,}976 = 0.0039$ ms。兩者不一致。
> 兩個值都與 decode 的 492 ms/步 相差約五個數量級，**故「五個數量級」的結論不受影響**，
> 但這一格數字建議回頭核對。

### 2.4 三種工作負載型態的有效動作空間不同

| 型態 | 決策點 | 預測的對象 | 有效動作空間 |
|---|---|---|---|
| **prefill 為主** | 請求**之間**保留哪些前綴 | 該前綴是否再被請求（時序） | 六階全部 |
| **多輪對話** | 同上，另含思考空窗期的保留成本 | 同上 | 六階全部 |
| **生成為主** | 請求**之內**哪些 block 可離開 GPU | 該 block 的 attention 分數（語意） | **僅 GPU 內的精度階**；CPU/SSD 慢 3–59× |

論文第 6 章的實驗涵蓋前兩種：工作負載為「同一前綴送兩次」，prompt 與生成比 1092:1，
故 **99.8% 的時間落在 prefill**。第三種是不同的預測問題，本文不處理。

### 2.5 三條精度軸：什麼是「儲存」、什麼是「計算」

「精度」在論文裡出現在**三個不同的地方**，混淆會直接讀錯主張。

| 軸 | 取值 | 是本文的決策變數嗎 | 由誰決定 |
|---|---|---|---|
| ① **權重**精度 | BF16 / FP8 / INT8 / AWQ-INT4 | ❌ **不是**。論文明言「將 LLM 權重視為 frozen，且不修改其精度」 | 部署者（列為敏感度分析） |
| ② **KV 儲存**精度 | BF16 / FP8 / INT8 / INT4（`--kv-cache-dtype`） | ✅ **是**。式 (4) 動作空間的 GPU 三階 | 本文的策略 |
| ③ **計算（算術）**精度 | **BF16/FP16 乘法 + FP32 累加** | ❌ 不是。**全程不變** | 硬體與 kernel |

> **一句話：目前所有量測與成本模型，算術一律是 BF16/FP16 乘法搭配 FP32 累加。
> INT4/FP8/INT8 全部只是儲存格式，讀取時反量化，沒有一處是低精度算術。**

三個推論：

1. **`Gpu₈` 是儲存狀態，不是運算狀態**（§6.9 明講）。
   KV 以 FP8 存放、**讀取時反量化**回 BF16 再做 attention，**與 tensor core 是否原生支援 FP8 無關**。
   這正是 Ampere（sm_86，確無原生 FP8 tensor core 運算）仍能實測 `--kv-cache-dtype fp8` 的原因——
   實測 Llama-3.1-8B 的 KV 容量由 41,648 增至 83,312 token，**恰好兩倍**，位元組佔用不變。

2. **AWQ-INT4 是 weight-only 量化**：權重以 INT4 存放，GEMM 前反量化，activation 不變。
   所以 `qwen-awq` / `llama-awq` 兩個剖面省下的是**記憶體預算**（§6.9：KV 預算自 5.9 GB 增至約 15 GB），
   **不是算術精度**。權重精度之所以進得了 go/no-go 判定，走的是式 (12) 的 $\beta$
   ——BF16 權重讓 decode 的固定成本變大，把 headroom 稀釋掉。

3. **表 4 / 表 5 的算力全部取 dense BF16 峰值**：
   RTX 3090 = 71.2 TFLOPS（$328\ \text{TC}\times1,695\,\text{MHz}\times128\,\text{FLOP/clk}$，
   **FP32 累加**下的數字，§A.4）、MI300X = 1,307.4、H100 = 989.4。
   $\kappa$ 與 FLOP/byte 都建立在這個基準上，且假設 **100% MFU** → **表 5 的 $\kappa$ 是下界**。

**反量化的成本記在哪裡**：表 14 把它放在「被需要時的成本」欄——
`Gpu₈`/`Gpu₄` 是「**反量化 kernel**」，而 `Cpu` 是「PCIe（**無反量化**）」。
這是無損位置階與有損精度階在成本結構上的另一個差異。

**唯一一處真正的硬體限制**（§6.6，論文標為硬體相依）：
`fp8_per_token_head` 需要 **Triton attention backend**，而該後端在 compute capability 8.6 上
**拒絕** FP8 KV cache。所以平台 A 的精度階**實際只有一級可用：BF16 → INT8**。
這不是算術精度的問題，是**後端支援**的問題——也是「同一個動作在不同硬體上是否存在，
本身即隨平台而變」的又一個實例。

### 2.6 精度與成本在哪裡交會

精度不是獨立的一條軸——它**同時**改變式 (7) 的兩項成本，而且方向相反。
這一節把「精度」與「成本」接起來，細節見 §4 式 (7) 與附錄 B。

#### (a) 平時成本（閒置時）：精度改變的是**位元組**

對應式 (7) 的第 ① 項 $\lambda_m \cdot \mathrm{mem}(i,a)$。這是精度階**唯一的收益來源**。

| KV dtype | KiB/tok | 理想值 | 額外 | vs BF16 |
|---|---|---|---|---|
| `auto` (BF16) | 128.11 | 128 | $+0.11$ | 1.00× |
| `fp8` | 64.04 | 64 | $+0.04$ | **2.00×** |
| `int8_per_tok_head` | 66.04 | 64 | $\mathbf{+2.04}$ | 1.94× |
| `int4_per_tok_head` | 34.02 | 32 | $\mathbf{+2.02}$ | **3.77×** |

**`fp8` 是純格式轉換故恰為 2.00×；兩個 per-token-head 量化階則各固定多出 2 KiB/token。**
此開銷可由架構直接算出（Llama-3.1-8B：$2\times32\times8 = 512$ 個 scale × FP32 4 bytes = 2,048 bytes），
與實測相符（int8 為 4.1 bytes/scale、int4 為 4.0）。

> **對成本模型的意涵**：若式 (1) 只以「每元素位元組數」計算，會把 INT8 算成 2×、INT4 算成 4×，
> **系統性高估低精度階的收益**。**量化中繼資料是與序列長度成正比的固定成本，必須進入成本模型。**

#### (b) 被需要時的成本：精度改變的是**反量化 kernel**

對應式 (7) 的第 ③ 項 $p_i \cdot [\mathrm{fetch} + \mathrm{recomp}]$。表 14 的右欄即為此項：

| 動作 | 平時成本（閒置） | **被需要時的成本** |
|---|---|---|
| `Gpu₁₆` | GPU 位元組 | $\approx 0$ |
| `Gpu₈` | GPU 位元組 / 2 | **反量化 kernel** |
| `Gpu₄` | GPU 位元組 / 4 | **反量化 kernel** |
| `Cpu` | host 位元組 | PCIe（**無反量化**） |
| `Ssd` | 磁碟位元組 | NVMe I/O + PCIe |
| `Drop` | **0** | 重算$(\ell)$，隨位置線性成長 |

**精度階與位置階的成本結構因此本質不同**：
精度階付的是**計算**（反量化），位置階付的是**頻寬**（PCIe / NVMe），`Drop` 付的是**重算**。
$\kappa$（表 5）只涵蓋**加速器與主機之間的傳輸**，**未涵蓋磁碟階，也未涵蓋反量化**。

#### (c) 第三項成本：精度階付的是 $\epsilon$，而它不是純量

位置階（`Cpu`/`Ssd`）只搬位元組，§6.6 以 60/60 逐字元比對驗證**無損**，$\epsilon = 0$。
**所以 $\epsilon$ 的實質內容全部落在精度那三階上**——精度階真正的代價不是位元組也不是 kernel，
而是**品質**，而 §6.6 量到它是「**精度 × 任務**」的函數：

| KV 精度 | 容量 | GSM8K（推理） | 大海撈針（檢索） | LongBench（理解） | RULER（整合） |
|---|---|---|---|---|---|
| BF16 | 1.00× | 77.9 | **100.0** | **58.4** | **91.6** |
| FP8 | 2.00× | 76.2 | **5.0** | **6.6** | **3.3** |
| INT8 | 1.94× | 76.4 | **95.0** | **57.0** | **79.1** |
| INT4 | 3.77× | 75.1 | **0.0** | **0.4** | **0.0** |
| **全距 (pp)** | — | **2.8** | **100.0** | **58.0** | **91.6** |

**同一組設定，換個任務型態，$\epsilon$ 差 21 至 36 倍。**
故式 (14) 取**最差任務**的保留率而非跨任務平均——其必要性由這張表直接證成。

**主因是格式而非縮放方式**：FP8 靜態縮放的相對誤差 2.64%，改用 per-token-head 動態縮放
僅降至 2.56%；INT8 動態縮放為 0.65%。**FP8 e4m3 的 3 個尾數位元把相對精度鎖在約 12.5%，
縮放方式無法改變**；INT8 於指定範圍內有 255 個刻度。
理論值（FP8 $12.5\%/\sqrt{12}=3.6\%$、INT8 $3/440=0.68\%$）與實測吻合，
且數值誤差可**單調預測**檢索結果：$0.65\% \to 95\%$、$2.64\% \to 5\%$、$11.76\% \to 0\%$。

#### (d) ⚠️ 目前所有 headroom 數字**都不含精度階**

這是讀第 6 章時最容易誤會的一點。§6.2 明講：

> oracle **只在 `Gpu₁₆`、`Cpu`、`Ssd`、`Drop` 上求解，未納入三個精度階**。
> 這四個動作已驗證為逐字元無損，故 $\epsilon$ 約束在此自動滿足，
> 本節的 headroom 是**位置分層本身**在 $\epsilon = 0$ 下可得的空間。

**精度階能額外帶來多少空間，本文未量。**
所以：8.42% / 31.8–33.5% 這些數字，是**四個無損動作**的成果，
把 `Gpu₈`/`Gpu₄` 加回去只會更高（但也要付 (c) 的品質代價）。

#### (e) 成本常數本身也隨剖面與裝置變動

**成本常數不可跨剖面替代**（表 12，單位 ms/block）：

| | `qwen-awq` NVMe（主設定） | `llama-bf16` NVMe | `llama-bf16` SATA（對照） |
|---|---|---|---|
| 量測 ctx | 96,000 | 16,384 | 16,384 |
| `Cpu` | 0.298 | 0.543 | 0.588 |
| `Ssd` | 10.245 | 6.286 | 5.536 |
| `Drop` 基底 $C^0$ | 3.546 | 4.008 | 4.008 |
| `Drop` 斜率 $\alpha$ | 0.000178 | 0.000210 | 0.000210 |
| **$P^{*}$（token）** | **37,717** | 10,851 | 7,278 |
| 位置擬合上限 | **258,048** | 24,576 | 24,576 |

**$P^{*}$ 在固定裝置上換模型剖面即差 3.5×，換磁碟裝置再得 1.5×——兩項皆發生在同一台機器上，
未換加速器。** 這是 $\kappa$ 主張的一個單平台實例：
**動作成本的比值不只隨加速器世代變動，在固定加速器上也隨模型剖面與儲存裝置變動。**

**且磁碟階的成本有 85% 不是 I/O**（§A.2）：兩顆碟裸讀差 4.0×（SATA 522 / NVMe 2,085 MiB/s），
端到端有效頻寬卻只差 1.1×（353 / 311 MiB/s，僅達各自裸讀的 68% 與 15%），
其餘是分層機制的查表、cascade 排程與 promotion 開銷 → 故 $C_{\text{ssd}}$ 分解為
$C_{\text{lookup}} + C_{\text{sched}} + C_{\text{io}}(\text{device})$，**只有末項與裝置有關**。

#### (f) 成本模型漏掉的一項：**寫入**頻寬

模擬的成本模型**只對「取回」計價**，而寫入亦消耗頻寬。§6.7 是為了補這個洞：

| 剖面 | trace | 最佳 baseline | `tier_fs` | **Oracle** |
|---|---|---|---|---|
| `llama-bf16` | toolagent | `tier_fs` | 4,666 | **1,114** |
| `llama-bf16` | conversation | `tier_fs` | 4,892 | **1,214** |
| `qwen-awq` | toolagent | `cpu_arc` | 2,016 | **283** |
| `qwen-awq` | conversation | `cpu_arc` | 2,122 | **330** |

（單位 MiB/s 持續寫入，512 GiB 磁碟階，Mooncake trace）

**Oracle 所需的寫入頻寬是 `tier_fs` 的 $1/4.0$ 至 $1/7.1$**，
此比值在兩個剖面、兩個 trace 上皆成立，是該節**唯一不隨剖面改變的結果**。

**論文明確不宣稱磁碟階於 NVMe 上不可部署**——該判定需要 NVMe 的持續寫入量測，而 §A.2 記錄「沒有做」
（SATA QLC 短測 492 → 長測只剩 **181 MiB/s，僅 37%**，短測整個落在 SLC 快取內；
NVMe 短測 2,512 MiB/s，**持續寫入為 `NOT_MEASURED`**）。
可宣稱者只有兩點：**寫入頻寬是磁碟階一道獨立於延遲的可行性約束，而五個 baseline 無一將其納入設計；
成本感知放置把這道約束放寬 4–7 倍。**

---

## 3. 四個觀察：動機章的量化事實

### 觀察一：單請求容量壓力在 MI300X 上幾乎不存在（但在 24 GB 級加速器上成立）

表 3（BF16 權重 + BF16 KV，保留 10% 餘裕）：

| 模型 | ctx | KV | H100 80GB | MI300X 192GB |
|---|---|---|---|---|
| Llama-3.1-8B | 128K | 15.6 GB | ✓ | ✓ |
| Llama-3.1-8B | 256K | 31.2 GB | ✓ | ✓ |
| **Llama-3.1-8B** | **1M** | **122 GB** | ✗ | **✓** |
| Qwen2.5-7B-1M | 1M | 53.4 GB | ✓ | ✓ |
| Llama-3.1-70B | 128K | 39.1 GB | ✗ | ✗ |

三個結論：**(a)** 8B–14B 模型於 128K 只吃 MI300X 的 8–10% HBM，
**既有文獻用以立論的場景在此硬體上量測不到**。
**(b)** Llama-3.1-8B 的 1M 上下文可完整置入單張 MI300X，而 H100 不能。
**(c)** 壓力沒有消失，只是**同一條壓力軸上的位置不同**——192 GB 只是把耗盡的長度往後推。

### 觀察二：卸載的相對代價隨硬體世代惡化

表 4：

| | Mem BW | Host link | **Mem:link** | FLOP/byte |
|---|---|---|---|---|
| RTX 3090 | 0.94 TB/s | 31.5 GB/s | **30:1** | 2,254 |
| H100 SXM | 3.35 TB/s | 64.0 GB/s | 52:1 | 15,459 |
| H200 SXM | 4.8 TB/s | 64.0 GB/s | 75:1 | 15,459 |
| MI300X | 5.3 TB/s | 63.0 GB/s | **84:1** | **20,752** |
| MI355X | 8.0 TB/s | 63.0 GB/s | **127:1** | — |
| GH200 | ~4 TB/s | 450 GB/s | **~9:1** | — |

**惡化的原因是結構性的**：HBM 容量與頻寬逐代提升，而 PCIe Gen5×16
自 MI300X 到 MI355X **完全未變**。
GH200 的 9:1 是例外（NVLink-C2C 450 GB/s），其卸載經濟性優於任何 x86 主機的加速器 7–14 倍。

> **RTX 3090 的 71.2 TFLOPS 怎麼來的**（§A.4）：$328\ \text{TC} \times 1,695\,\text{MHz} \times 128\,\text{FLOP/clk}$。
> GA10x 的 tensor core 於 **FP32 累加**下為 128 FLOP/clk（FP16 累加為 256），BF16 與 FP16-FP32 累加同速。
> **常見錯誤是取 35.6 TFLOPS**——那對應 64 FLOP/clk，是 TF32 tensor 或非 tensor FP32 的峰值，資料路徑不同。

### 觀察三：重算的價值不在單次成本（§2.4，本文的軸心）

**正確的判準是 FLOPS 對主機鏈路之比，不是 HBM 頻寬比。**
MI300X 的 dense BF16 峰值 1307.4 TFLOPS ÷ 63 GB/s → 每傳輸一位元組可換 ~20,750 FLOP；
H100（989.4 TFLOPS）為 15,460。MI300X 在此指標上優於 H100 約 1.34 倍。

但**絕對值上傳輸仍然便宜，且便宜的程度隨平台劇烈變動**（表 5，簡化 prefill 模型
$2N_{\text{params}}$ FLOP/token、100% MFU）：

| 平台 | 模型 | 重算 | 傳輸 | $\kappa$ |
|---|---|---|---|---|
| MI300X | Llama-3.1-8B | 12.2 μs | 2.08 μs | **6×** |
| MI300X | Llama-3.1-70B | 107 μs | 5.20 μs | **21×** |
| RTX 3090 | Llama-3.1-8B | 225 μs | 4.16 μs | **54×** |
| RTX 3090 | Llama-3.1-70B | 1,972 μs | 10.4 μs | **190×** |

$190/6 \approx 32$ → **$\kappa$ 跨平台變動 32 倍**，這是「硬體條件化策略」最直接的量化依據。

**$\kappa$ 也隨模型架構變動，且方向可事先計算。** $\kappa$ 的分子由**啟用**參數量決定、
分母由 KV 大小決定，而 **MoE 使兩者解耦**：KV 由 attention 層決定（同規模 dense 相同），
但重算只需啟用的專家。Qwen3-30B-A3B（48 層、4 KV head、128 專家啟用 8、啟用參數約 3B）
在 RTX 3090 上 $\kappa = 27$，**僅為同平台 dense 模型（48–108）的約一半**。
→ 可證偽的預測：**固定平台上，MoE 模型的最佳 `DROP` 觸發門檻應高於 dense 模型。**

**圖 1 的核心（成本反向增長）**：

```
狀態             平時成本（閒置）        被需要時（μs/token）
GPU · BF16       ██████████ 最高         0
GPU · FP8        █████ 1/2               ≈0
GPU · INT4       ██▌ 1/4                 ≈0
CPU DRAM         █▎ 低                   █ 1.0
SSD              ▌ 極低                  ████████ 9
DROP → 重算      （零）                  ███████████ 12.3
```

**兩側長條的長度即為成本，其反向增長是本文的出發點。**
→ 重算的正確條件是「該 block 未來被存取的機率**足夠低**」——
低到節省的容量與頻寬能抵償萬一被需要時 6–190 倍的代價。
→ **未來效用預測不是附加元件，而是決定重算動作正確與否的唯一依據。**

### 觀察四：雙平台設計為什麼是必要的

表 4 的 FLOP/byte 跨越一個數量級：RTX 3090 = 2,254、MI300X = 20,752，**相差 9.2 倍**。
**單一平台無法檢驗硬體條件化的主張**，因為該主張的內容正是「常數改變時最佳策略隨之改變」。

兩個平台承擔的是**同一條壓力軸的兩端**：
RTX 3090（24 GB）可推至 8B 模型的約 262K 上下文；MI300X（192 GB）延伸至更大的模型與 1M 級上下文。
論文承認此二平台位於光譜的**中段而非兩端**（GH200 為 9:1、MI355X 為 127:1），
可證偽性主張因而受限於實際可取得的硬體跨度。

---

## 4. 全部 14 條公式，逐條解釋

> 編號與 `main.pdf` 一致。未編號者以「(§節)」標示。

### 式 (1)　KV cache 每 token 的記憶體佔用（§2.1，p.2）

$$
b_{\text{tok}} = 2 \times L \times H_{kv} \times d_{h} \times s
$$

| 符號 | 意義 |
|---|---|
| 前導 2 | 對應 **K 與 V 兩份** |
| $L$ | 層數 |
| $H_{kv}$ | **KV head 數**（GQA 下小於 query head 數） |
| $d_h$ | head dimension |
| $s$ | 每元素位元組數（BF16 = 2） |

**驗算**：Llama-3.1-8B $= 2\times32\times8\times128\times2 = 131,072$ bytes $= 128$ KiB/token。

表 2 的六個模型：

| 模型 | $L$ | $H_{kv}$ | $d_h$ | KV/tok |
|---|---|---|---|---|
| Qwen2.5-7B-Inst-1M | 28 | 4 | 128 | **56 Ki** |
| Llama-3.1-8B-Inst | 32 | 8 | 128 | 128 Ki |
| Qwen3-8B | 36 | 8 | 128 | 144 Ki |
| Qwen3-14B | 40 | 8 | 128 | 160 Ki |
| Qwen3-30B-A3B (MoE) | 48 | 4 | 128 | 96 Ki |
| Llama-4-Scout (MoE) | 48 | 8 | 128 | 192 Ki |

**⚠️ 這條式子的陷阱（§6.9 的量測修正）**：
若只用「每元素位元組數」計算低精度階的收益，會**系統性高估**。
per-token-head 量化每 token 固定多帶 **2 KiB 中繼資料**（Llama-3.1-8B：
$2\times32\times8 = 512$ 個 scale × FP32 4 bytes = 2,048 bytes）。
故實測收益是 **2.00× / 1.94× / 3.77×**，而非 2× / 2× / 4×。
**量化中繼資料是與序列長度成正比的固定成本，必須進入成本模型。**

---

### 式 (2)　重算成本對 block 絕對位置為**線性**（§2.4，p.3）

$$
C_{\text{recompute}}(P) \;=\; 513.0\ \text{ms} \;+\; 26.9\ \text{ms}\times\frac{P}{1000}
$$

**這是全篇最重要的修正之一。** 論文原本假設它是二次的，量測顯示是線性的。

**為什麼是線性**：整段 prefill 的總成本**確實**是序列長度的二次式（attention 為 $O(L^2)$）。
但本文的動作空間作用於**固定大小的 block**：在位置 $P$ 重算一個大小為 $C$ 的 block，
其 attention 只需讀取前序的 $P$ 個 token，成本為 $O(C \cdot P)$ ——**對 $P$ 為線性**。

**量測**（平台 A，$C = 2048$，每點三次取中位數）：
- `llama-bf16`：$P \in \{0, 4\text{K}, 8\text{K}, 16\text{K}, 24\text{K}\}$，最大偏差 **1.6%**，
  五點皆落於直線上，**未觀察到二次成分**；$P=0 \to 24,576$ 成本成長 **2.29×**。
- `qwen-awq`：另有 **11 個位置**（$P \le 258,048$），線性形式在放大 $10.5\times$ 的位置範圍上仍成立，
  成本自 **453.9 ms** 成長至 **6,320.2 ms**（$13.9\times$）。

**係數依平台與模型而定（表 12），但「線性」這個形式來自 $O(C\cdot P)$，與平台無關。**
→ **將重算視為單一常數，會在長上下文下系統性低估其成本。**

---

### 式 (3)　系統狀態（§4.1，p.6）

$$
s_t = \Big( \{ \ell_i, \pi_i, a_i, n_i \}_{i \in \mathcal{B}},\; C^{\text{gpu}}, C^{\text{cpu}}, C^{\text{ssd}},\; Q_{\text{pcie}},\; \phi_t \Big)
$$

| 分量 | 意義 |
|---|---|
| $\ell_i$ | block $i$ 的**所在層級**（GPU/CPU/SSD/不存在） |
| $\pi_i$ | 其**精度**（BF16/FP8/INT4） |
| $a_i$ | **上次存取時刻** |
| $n_i$ | block 內的 token 數 |
| $C^{\ast}$ | 各層剩餘容量 |
| $Q_{\text{pcie}}$ | **互連佇列深度**（PCIe 壅塞程度） |
| $\phi_t$ | 當前查詢特徵 |

**注意**：$\ell$ 與 $\pi$ 分開，正因為位置與精度是**兩個獨立維度**（§2.2）。
既有工作各只管一個，本文把它們合成單一階梯。

---

### 式 (4)　動作空間（§4.1，p.6）

$$
\mathcal{A} = \{\, \textsf{Gpu}_{16},\; \textsf{Gpu}_{8},\; \textsf{Gpu}_{4},\; \textsf{Cpu},\; \textsf{Ssd},\; \textsf{Drop} \,\}
$$

**六個狀態逐一說明**：

| 動作 | 平時成本（閒置） | 被需要時的成本 | 有損？ |
|---|---|---|---|
| `Gpu₁₆` | GPU 位元組（最高） | $\approx 0$ | 無損 |
| `Gpu₈` | GPU 位元組 / 2 | 反量化 kernel | **有損** |
| `Gpu₄` | GPU 位元組 / 4 | 反量化 kernel | **有損** |
| `Cpu` | host 位元組 | PCIe（無反量化） | **無損** |
| `Ssd` | 磁碟位元組 | NVMe I/O + PCIe | **無損** |
| `Drop` | **0** | 重算$(\ell)$ | 無損（重算得回原值） |

**三個必須記住的細節**：

1. **`Cpu` 與 `Ssd` 儲存未經壓縮的位元組。** 它們只改變 block 的位置，不改變內容。
   此無損性於 §6.6 以逐字元比對驗證（**60/60**）。**精度維度因而只存在於 GPU 常駐的三階，
   $\epsilon$ 約束的實質內容全部落在該三階上。**
2. **階梯是示意，量化器可替換。** 實務上 **key 對量化誤差遠比 value 敏感**，
   故生產系統普遍採 K/V 分離配置（LMCache 預設 `turboquant_k8v4` = K 8bit / V 4bit；
   KIVI 對 key 採 per-channel、對 value 採 per-token）。
   **本文的決策框架與此正交**：任何量化器只要給出「容量佔用」與「存取延遲」兩個數字即可當一階，
   換掉某一階**不改變式 (8) 的形式，只改變成本常數**。
3. **`Drop` 是階梯上的一層，而非檢索失敗後的補救。** 這是與既有工作最大的結構差異。

---

### 式 (5)　`Drop` 的可行性受前序 block 約束（§4.1，p.6）

$$
a_i = \textsf{Drop}\ \text{可行} \iff \forall j < i:\ \text{block } j \text{ 的 KV 可取得}
$$

**其餘五個動作彼此獨立**——一個 block 放哪一層不影響其他 block 能否取回。
**`Drop` 是唯一的例外**，因為重算 block $i$ 時 attention 要讀前序 block 既有的 KV。

---

### 式 (6)　丟棄的依賴鏈成本（§4.1，p.6）

$$
\mathrm{recomp}(i) = \sum_{j \in \mathcal{D}(i)} \Big( 2 N_{\text{active}} |b_j| + \mathrm{attn}(j) \Big),
\qquad
\mathcal{D}(i) = \{i\} \cup \{\, j < i : a_j = \textsf{Drop} \,\}
$$

若前序 block 也被丟棄，必須**先重算它們**，成本沿依賴鏈累積。

- $2 N_{\text{active}} |b_j|$：線性層的 FLOP（每 token 約 $2N_{\text{params}}$；MoE 用**啟用**參數量）
- $\mathrm{attn}(j)$：該 block 的 attention 成本，即式 (2) 隨位置線性成長的那一項

**意涵：`Drop` 決策彼此耦合，不可獨立最佳化。**
一個只看單一 block 效用的策略會**系統性低估連續丟棄的代價**。
本文以「同一序列內連續 `Drop` 的 block 數上限」作為可行性約束處理。

> 這也正是 **HCache** 儲存 hidden state 的動機：由 hidden state 可**單步**重建 KV，
> 從而**切斷依賴鏈**。

---

### 式 (7)　單一動作的即時成本（§4.2，p.6）— **四項成本在此同時出現**

$$
c(i,a) = \underbrace{\lambda_{m} \cdot \mathrm{mem}(i,a)}_{\text{① 容量佔用}}
        + \underbrace{\lambda_{x} \cdot \mathrm{xfer}(i,a)}_{\text{② 搬移成本}}
        + \underbrace{p_i \cdot \big[\, \mathrm{fetch}(i,a) + \mathrm{recomp}(i,a) \,\big]}_{\text{③ 期望存取成本（含④ 未來效用）}}
$$

| 項 | 內容 | 既有工作誰有 |
|---|---|---|
| ① 記憶體成本 | $\lambda_m \cdot \mathrm{mem}$ | EvicPress |
| ② 傳輸成本 | $\lambda_x \cdot \mathrm{xfer}$ | CacheGen 一系 |
| ③ 重算成本 | $\mathrm{recomp}$，僅 $a=\textsf{Drop}$ 時非零，且**隨絕對位置成長**（式 2） | Cake / KVPR / HCache |
| ④ 未來效用 | $p_i = \Pr[\text{block } i \text{ 於未來視窗內被存取}]$ | Marconi / KVP |

**論文的貢獻宣稱就在這裡**：表 6 的特徵矩陣顯示，**四項從未同時進入單一線上決策**。
現有最高覆蓋為 **Marconi 的三項**（未來效用、計算節省、記憶體佔用），
其**因僅在 GPU 上運作而結構性地不可能具備傳輸成本**。

---

### 式 (8)　受品質約束的最佳化問題（§4.3，p.6）

$$
\begin{aligned}
  \min_{\{a_i\}} \quad & \sum_{i \in \mathcal{B}} c(i, a_i) \\
  \text{s.t.} \quad & \Delta\mathcal{Q}\big(\{a_i\}\big) \le \epsilon &\text{(品質為\textbf{硬性約束})}\\
                    & \textstyle\sum_i \mathrm{mem}^{\text{gpu}}(i,a_i) \le C^{\text{gpu}} &\text{(對 CPU/SSD 同理)}\\
                    & \mathrm{TTFT} \le T_{\text{SLO}},\;\; \mathrm{TPOT} \le P_{\text{SLO}} &\text{(服務等級)}\\
                    & \text{式 (5) 的可行性約束} &\text{(Drop 依賴鏈)}
\end{aligned}
$$

**與既有工作的三點差異**：
1. 動作空間 (4) **同時跨越位置與精度兩個維度**，並把 `Drop` 視為一個**具明確標價的層級**而非失敗路徑；
2. 品質是**硬性約束**而非目標函數中的加權項（與 KVServe 一致，但作用於**放置**而非壓縮）；
3. 成本函數 (7) 中**四項成本同時出現**。

**$\epsilon$ 的可操作性（本設計最主要的風險點）**：
$\Delta\mathcal{Q}$ 於執行期**不可直接觀測**，這是所有品質約束工作的共同困難。
KVServe 採離線 profiling + $\epsilon$-greedy bandit 修正。本文規劃兩種代理訊號：
(a) 離線 profile 建立的 (壓縮率, 任務類型) → 品質查找表；(b) 模型輸出分佈的信心值作為線上回饋。

---

### 式 (9)　最佳決策門檻 — **本文損失函數論證的支點**（§5.3，p.10）

$$
p^{*} = \frac{c_{\text{FP}}}{c_{\text{FP}} + c_{\text{FN}}} = \frac{1}{1+\kappa}
$$

**推導**（兩行）：
- 保留的期望成本 $= (1-p)\,c_{\text{FP}}$（預測會用 → 保留 → 實際沒用 → **只浪費容量**）
- 丟棄的期望成本 $= p\,c_{\text{FN}}$（預測不會用 → `Drop` → 後來被需要 → **被迫重算**）
- 兩者相等處即為最佳門檻。令 $\kappa = c_{\text{FN}}/c_{\text{FP}}$ 即得上式。

**這是成本敏感學習的標準結果（Elkan 2001），論文不主張其為貢獻。**
論文主張的是它在本設定下的**意涵**：

> $\kappa = 10$ 時 $p^{*} = 1/11 \approx 0.09$。
> **一個以對稱損失訓練、並在 0.5 處閾值化的預測器，其決策門檻被錯置了約一個數量級。**

**更具體的主張**：
**既有所有學習式 KV 管理方法的動作空間都是二元且成本同質的（$\kappa=1$、$p^{*}=0.5$），
對稱損失恰好正確；而本文的動作空間成本異質且 $\kappa$ 由硬體決定，對稱損失因而失準。**

各設定下的 $p^{*}$：MI300X/8B（$\kappa=6$）→ $0.14$；平台 A/70B（$\kappa=190$）→ $0.005$。

---

### 式 (10)　(L1) 動作成本加權的 L2 損失（§5.3，p.10）

$$
\mathcal{L}_{\text{L1}} = \sum_i w(a_i) \cdot \big( \hat{y}_i - \log \tau_i \big)^2,
\qquad
w(a_i) = \frac{\mathrm{cost}_{\text{err}}(a_i)}{\mathrm{cost}_{\text{err}}(\textsf{Cpu})}
$$

在 $\log(\text{距下次存取時間})$ 的迴歸上，令每個樣本的權重 = 「對該 block 所採取的動作，
判斷錯誤時的代價」。**在 LightGBM 中僅需 `sample_weight`，無額外機制。**

**⚠️ 論文自己指出的校準限制**：
對連續目標的迴歸加權，改變的是**條件期望** $\mathbb{E}[\log \tau \mid x]$，**而非決策門檻**。
(L1) 本身因而**不保證**產生式 (9) 的最佳邊界。
論文把 (L1) 定位為**啟發式**，並在其後串接一個明確的**門檻校準步驟**：
於驗證集上以 isotonic regression 把預測值映射為 $\hat p$，再依式 (9) 設定各層級門檻。

**本文為此設了一個誠實的基線**（表 15 的 (B) 區塊）：
「**對稱 L2 @ $p^{*}$**」——以對稱損失訓練，只在決策時把門檻從 0.5 移到 $p^{*}$。
這正是 Elkan 的結果，且有一個實際優點：**$\kappa$ 隨平台改變時無需重新訓練**。
**若本文的增益全部來自門檻移動，則加權訓練並無貢獻。**

**論文預期加權訓練仍有增益，理由是**：Elkan 的結果成立於「機率估計在整個值域上等品質」的前提；
有限容量的模型不滿足此前提，它必須決定把擬合能力配置到哪裡。
對稱損失依樣本密度均勻配置，而決策邊界 $p^{*}$ 隨 $\kappa$ 增大而**深入機率分佈的尾端**
（$\kappa=190$ 時 $p^{*}=0.005$）——**在該處對稱損失幾乎沒有誘因把它學準**。

**可證偽的預測**：**(B) 區塊中「對稱 L2 @ $p^{*}$」與 (L1)/(L2) 的差距，應隨 $\kappa$ 增大而擴大。**
若兩者在 3090 與 MI300X 上差距相同，加權訓練的正當性不成立，論文將縮減主張至門檻校準。

---

### 式 (11)　(L2) 動作成本進入排序損失的 gain 項（§5.3，p.10）

$$
\mathrm{DCG} = \sum_{w} \frac{\mathrm{cost}_{\text{err}}(a_w) \cdot \big(d(l_w) - 1\big)}{\log\big(\mathrm{pos}(l_w) + 1\big)}
$$

**出處與差異**：**PARROT** 是本文調查中唯一刻意使用**非對稱損失**的工作，
其 NDCG 形式明確「重罰把機率放在即將被重用的 line 上，輕罰放在遠期重用的 line 上」，帶來 3.5% 命中率提升。
**但 PARROT 只有單一動作**，其 gain 項 $(d(l_w)-1)$ 只編碼**重用距離**，未編碼**動作成本**。
本文把 $\mathrm{cost}_{\text{err}}(a_w)$ 乘進 gain 項。

---

### 式 (12)　decode 每步成本的線性模型（§6.5，p.13）

$$
t_{\text{decode/step}} = \beta + \gamma \cdot N_{\text{blocks}}
$$

- $\beta$：**固定成本**，包含**讀取權重**（所以 BF16 權重會讓它變大）
- $\gamma \cdot N$：**讀取 KV 的成本**，與 block 數線性

**實測擬合（$R^2 = 0.999$）**：
- `llama-bf16`：$36.768 + 0.005581\,N$ ms
- `qwen-awq`：$18.158 + 0.001267\,N$ ms

**物理合理性檢查**：$\gamma$ 換算之 KV 讀取頻寬分別為 **376** 與 **581 GB/s**，
皆低於 RTX 3090 的 **936 GB/s** 峰值 → 通過。

**這條式子撐起 go/no-go 判定的第一個條件**：
decode 佔端到端時間 48.8%（toolagent）/ 53.8%（conversation），
而**於 `llama-bf16` 剖面升至 75%**，因為 $\beta$ 裡的權重讀取變貴了。
**放置決策對 decode 完全沒有自由度**（每步都要讀完整 KV），
所以權重不量化 → headroom 被 decode 稀釋 → 掉到停止條件之下。
端到端 headroom ≈ prefill headroom × **0.88**。

---

### 式 (13)　`Ssd` 與 `Drop` 的成本交叉點（§6.8，p.14）

$$
P^{*} = \frac{C_{\text{ssd}} - C_{\text{recompute}}^{0}}{\alpha}
$$

- 分子：SSD 取回成本（**與位置無關的常數**）減去重算的**基底**成本
- 分母：重算成本對位置的**斜率** $\alpha$（式 2）

**幾何意義**：一條水平線（SSD）與一條斜線（DROP）的交點。
$P^{*}$ **之前** `Drop` 較廉，**之後** `Ssd` 較廉。

**為什麼這決定了 headroom 的形狀**：
- 請求長度 $\ll P^{*}$ → 所有 block 都該重算 → **策略無選擇餘地**
- 請求長度 $\gg P^{*}$ → 所有 block 都該搬移 → **策略無選擇餘地**
- **只有當 $P^{*}$ 落在請求內部，同一請求的不同位置才需要不同決策**

實測（表 12）：`qwen-awq`/NVMe **$P^{*} = 37,717$**、`llama-bf16`/NVMe **10,851**、
`llama-bf16`/SATA **7,278**。**在同一台機器上換模型剖面即差 3.5×，換磁碟裝置再得 1.5×。**

**$\alpha$ 的分母意義**：加速器越快 → $\alpha$ 越小 → $P^{*}$ 越大 → **峰值長度越長**。
以 MI300X 的 BF16 峰值算力比外推（$1307.4/71.2 = 18.4\times$）：
$P^{*} = $ 346K–693K，甜蜜點 $2$–$3.5\,P^{*}$ 落在 **692K–2.4M token**。

---

### 式 (14)　品質如何化約為單一數字（§B.6，p.22）

$$
Q(\pi) \;=\; \min_{t \in \mathcal{T}} \; r_t(\pi),
\qquad
r_t(\pi) = \frac{Q_t(\pi)}{Q_t(\textsf{FullGPU})}
$$

品質約束因而具體化為 $Q(\pi) \ge 1-\epsilon$。

**取「最差任務」的保留率，而非跨任務平均。** 理由是量測結果直接證成的：
平均會使**單一任務的崩潰**被其餘任務的分數掩蓋。
§6.6 量到 INT8 在 GSM8K、LongBench、大海撈針三項上都讀起來無損，
**卻在 RULER 的 `niah_multikey_3`（UUID 多鍵檢索）上只剩 53.3**。
Yandex 的研究亦報告 ShadowKV 等方法在 needle-in-a-haystack 表現良好卻在**多事實抽取**上失效。

> 逐任務的 $r_t$ 必須完整列於附錄，避免式 (14) 的化約隱藏任務間的差異。

---

### 未編號但重要的三條式子

**(a) EDC 遞迴**（§5.2，LRB 的特徵設計，也出現在演算法 1 第 2 行）：

$$
C_i \leftarrow 1 + C_i \cdot 2^{-\Delta_1 / 2^{9+i}}, \qquad i = 0 \ldots 9
$$

10 個指數衰減計數器，半衰期自 $2^9 = 512$ 到 $2^{18} = 262,144$。
**僅在存取時更新**，故服務時成本近乎零。
其功能與循環網路的隱藏狀態**同構**，差別在於前者的更新規則寫死、後者由訓練得到
（表 15 的 (D) 區塊 GRU 一列檢驗的正是此差別是否重要）。

**(b) 磁碟階成本的分解**（§A.2）：

$$
C_{\text{ssd}} = C_{\text{lookup}} + C_{\text{sched}} + C_{\text{io}}(\text{device})
$$

**只有末項與裝置有關。** 這是量測逼出來的：兩顆碟的裸讀相差 **4.0×**（SATA 522 / NVMe 2,085 MiB/s），
端到端有效頻寬卻只差 **1.1×**（353 / 311 MiB/s）→ **於 NVMe 上，磁碟階的成本有 85% 不是 I/O**，
其餘是分層機制的查表、cascade 排程與 promotion 開銷。

**(c) KVServe 的形式化**（§3.5，本文與之對照）：

$$
\arg\min_p T_p(c) \quad \text{s.t.}\quad T_p(c) \le T_{\text{SLO}} \wedge q_p(w) \ge q_{\min}
$$

是本文所知**唯一在 Tier-1 場所把模型品質寫為硬性約束**的 KV 工作，
**但其決策變數是壓縮 profile $p$，不含放置**。本文的式 (8) 把同樣的硬性約束作用於放置。

---

## 5. 兩個演算法，逐行解釋

### 演算法 1：預測器的服務時流程（每個 KV block 一次）— p.9

```
Require: block b、決策視窗 W、GBDT f、校準映射 g、各階成本比 κ_ℓ
Ensure:  動作 a ∈ 𝒜

── 特徵抽取（六族特徵，全部於服務時免費取得）──
1: deltas   ← 前 32 次存取的時間間隔                    ▷ 未填滿者為 ∞
2: edc[i]   ← 1 + edc[i]·2^(−Δ₁/2^(9+i)),  i=0..9      ▷ 僅於存取時更新
3: static   ← (block_size, layer_id, head_id, token_type)
4: pooled   ← block 內 key/value 向量的池化統計
5: attn     ← 最近 8 個解碼步中 b 取得的 attention 質量佔比  ▷ forward 已算出
6: φ        ← concat(deltas, edc, static, pooled, attn)

── 評分（迴歸連續量，不做二元分類）──
7: ŷ ← f(φ) = log τ̂                                    ▷ τ̂ 為距下次存取的時間
8: p̂ ← g(ŷ) = Pr[τ ≤ W]                                ▷ isotonic regression，驗證集上校準

── 依成本設門檻（門檻由式 (9) 給出，非 0.5）──
9:  for 每個階 ℓ，由廉至貴 do
10:     if p̂ ≥ p*_ℓ = 1/(1+κ_ℓ) then return a ← ℓ
11: end for
12: return a ← Drop
```

**逐段解釋**：

| 行 | 在做什麼 | 為什麼這樣做 |
|---|---|---|
| 1 | **deltas**：前 $k \le 32$ 次存取的時間間隔 | 沿用 LRB 的設計，**涵蓋 LRU、LRU-$K$ 與 S4LRU**——這些啟發式全是 deltas 的特例。未填滿者為 $\infty$，**GBDT 原生處理**。 |
| 2 | **EDC**：10 個指數衰減計數器 | 半衰期 512 到 262,144 的指數加權和。**僅在存取時更新**，故服務時成本近乎零。 |
| 3 | **靜態特徵** | `token_type` 的依據是 SAECache 量到不同語意類型（system prompt / 使用者查詢 / 工具輸出 / 推理鏈）的**重用率差異達 756 倍**，而該特徵**取得成本為零**。 |
| 4 | **pooled**：block 內 key/value 的池化統計 | 回應一個自我批評：「**不看 KV 而預測 KV 的重用站不住腳**」。KVP 明確只用 key/value 向量、LookaheadKV 透過層內 adapter 讀取、ForesightKV 蒸餾真實未來 attention——本文若只用存取歷史，作為服務層的低成本選擇合理，**作為學習貢獻則站不住腳**。 |
| 5 | **attn_mass**：最近 8 步的 attention 質量佔比 | 對**仍位於 GPU** 的 block，query 側訊號品質高於任何存取歷史特徵，捨棄它並無理由。**此量在 forward pass 中已被計算**，記錄它只需一次 per-block 規約，**不引入額外的 $q\cdot k$**。 |
| 7 | 迴歸 $\log \tau$ | **預測連續量，不做二元分類。** 所有預測連續量並延後閾值化的系統都勝過其二元分類前身（Mockingjay 與 LRB 均明確報告此點；LRB 曾嘗試對 Belady 邊界做二元分類並依 good-decision-ratio 拒絕之）。取對數的理由與 LRB 相同：**真正重要的是落在邊界的哪一側，100K 與 2M 的差異遠比 2M 與 3M 重要**。 |
| 8 | isotonic regression 校準 | **式 (9) 的最佳門檻只在 $\hat p$ 是校準過的機率時才成立。** 若模型輸出 $\hat p = 0.1$ 的樣本中實際重用比例並非約 10%，該門檻即失去意義，§5.3 的整個論證隨之失效。 |
| 9–12 | 由廉至貴逐階套門檻 | **門檻永遠施加於 $\hat p_i$，不施加於 $\hat y_i$**——對 $\hat y_i$ 直接閾值化在數學上不對應式 (9) 的最佳邊界。 |

**特徵集在階梯上呈「降級退化」形態**：

```
位於 GPU  → 存取歷史 + query 側訊號（attn_mass）+ pooled KV   ← 最完整
位於 CPU/SSD → 僅存取歷史（q·k 不可得，因為 k 不在 GPU 上）
已丟棄     → 僅靜態特徵
```

**GBDT 原生處理缺失值，此結構無須額外處理。**

**模型是單一全域 GBDT**，`layer_id` 與 `head_id` 作為**特徵**而非分模型的依據
（KVP 的 112 個獨立 agent 帶來可觀工程成本，而其論文**並未提供**
「per-head 模型優於帶 head-id 特徵的共享模型」的消融證據）。

---

### 演算法 2：離線 oracle（單趟重放）— p.12

**這是產生第 6 章全部 headroom 數字的工具。**

```
Require: trace T、成本模型 C、各階容量 cap_ℓ、規則 d ∈ {cascade, cost-aware}
1:  nxt[b]      ← b 的所有未來存取索引，∀b
2:  tail[r,p]   ← Σ_{k≥p} C_Drop(k·B)              ▷ 前綴語意下丟棄的邊際成本
3:  𝒢, 𝒞, 𝒮   ← ∅                                 ▷ GPU、CPU、SSD 階
4:  for 每個請求 r ∈ T do
5:      g ← 第一個未命中的位置                       ▷ vLLM 回傳 token 數，缺口後全數重算
6:      for 每個位置 p 與其 block b do
7:          if p > g then cost += C_Drop(p·B)
8:          else          cost += C_tier(b)(p·B)     ▷ 依 b 所在階計價
9:          𝒢 ← 𝒢 ∪ {b}
10:         while |𝒢| > cap_gpu do
11:             v ← argmax_{b∈𝒢} nextuse(b)          ▷ Bélády / MIN
12:             𝒢 ← 𝒢 \ {v}
13:             if nextuse(v) = ∞ then continue      ▷ 不再用到，丟棄成本 0
14:             δ ← tail[nextuse(v)]                 ▷ 丟棄 v 的邊際成本
15:             ok_ℓ ← (d = cascade) ∨ (C_ℓ < δ)
16:             依序試 𝒞、𝒮：可放則放；該階已滿則與其中最不划算者交換，
                被擠出者續試下一階；皆不可則丟棄 v
17:         end while
18:     end for
19: end for
```

**兩條目的地規則**（各跑一次取較佳者）：
- **cascade**：無條件往下推（逐層下推，即 `tier_fs` 的行為）
- **cost-aware**：**只在該階成本低於丟棄的邊際成本時才放**

**兩條都是可實作的離線策略，取其較佳者仍然可實作**——這是它能當 baseline 上界的前提。

#### oracle 付的價與線上策略相同（三道防作弊設計）

1. **重算按 block 的絕對位置計價**（式 2）。
   若讓 oracle 一律以位置 0 的價格重算，**它付的價會低於線上策略，等於 oracle 自己作弊**。
2. **前綴語意下丟棄一個 block 的邊際成本是它到請求結尾的整條尾巴**（第 2 行的 `tail`），
   而非它自己那一塊。
3. **逐出去向逐項計數**。若「不再用到、丟棄成本為零」的逐出（第 13 行）佔滿全部，
   則 CPU 與 SSD 階對最優解毫無貢獻，量到的 headroom 全部來自 GPU 階內的逐出選擇，
   **不足以支持多階動作空間**。

#### 所報 headroom 為**下界**

多階且動作成本異質的最佳放置**論文並未求解**。兩個偏弱之處：
- 第 16 行的**貪婪**選擇不保證全域最優；
- 逐出**未**做前綴感知——真正的最優解會刻意讓保留的 block 構成**連續前綴**以避免製造缺口，
  而本 oracle 一樣會被缺口懲罰。

**此不對稱使 GO 判定安全，NO-GO 判定則須額外審視。**
（與 Sibyl 報告「達到全知 oracle 的 80%」的評估傳統一致，論文以此上界作為**判準而非成績**。）

#### 實作細節與踩過的坑（§A.3）

- **GPU 階以 max-heap 搭配 lazy deletion 取 Bélády 犧牲者，每次逐出 $O(\log n)$。**
  先前的版本每次逐出**掃描整個 GPU 集合**，於 17,117 blocks 的預算與 20 萬次存取下
  約需 $3.5\times10^{9}$ 次比較而**無法完成**。
- CPU 階以 next-use 的 max-heap 保留下次使用最近者；
  SSD 階以「相對丟棄的節省量」min-heap 先換掉最不划算者。
  **被 CPU 階擠下來的 block 會再往 SSD 階試一次，而非直接丟棄。**

**三項不變量檢查**（跑完後自動執行，任一失敗即結果不得採用）：
1. **命中數守恆**：各階命中與重算之和 = 總存取數
2. **強制未命中下限**：重算次數不得低於不重複 block 數
3. **oracle 為上界**：oracle 的總成本不得高於任一線上策略

> **第 3 項曾在 2026-08-31 失敗**：SSD 階 2 TiB 時 oracle 的 headroom 為 **$-3.16\%$**，依定義不可能。
> 原因是成本感知規則以**單一 block** 而非整條尾巴估計丟棄成本，做出局部便宜、全域昂貴的決策。修正後全部通過。

**效力前提兩條**：
1. **成本常數須為實測**——模擬器不接受推估值，且要求 GPU 預算、KV 每 token 位元組數與成本常數
   **三者取自同一個模型剖面**，否則拒絕執行。
2. **模擬器須能複現已量測的行為**——`full_gpu` 相對 `cpu_lru` 的 warm 段成本比：
   16K 實測 9.00 vs 模擬 9.73（差 8%）、32K 實測 12.27 vs 模擬 12.66（差 3%）；
   絕對值亦相符（`full_gpu` 5,033 vs 5,864 ms、`cpu_lru` 559 vs 603 ms）。
   **驗證僅涵蓋一種工作負載形狀與兩個有鑑別力的上下文長度**，論文引用絕對值時一併標明此範圍。

**逐 block 的簡化對前綴結構的工作負載無害**：
vLLM 的查詢語意是**連續前綴**（`_lookup_complete_chunks` 回傳一個 token 數，
無法表達「block 0、1、4、5 命中，2、3 未命中」），故第一個缺口之後的 block 一律重算。
量化其影響：缺口之後的 block 中仍留在任一階的比例為 **0.000%–0.464%**，
其中 61.8%–80.1% 是首次出現的 block（本來就必須重算）。
Mooncake 的 `hash_ids` 就是前綴雜湊，**第一個未命中恰好落在共用前綴的結尾**。

---

## 6. 設計：預測器與成本敏感損失

### 6.1 系統定位：機制屬於 vLLM，策略屬於本文

**設計上的關鍵決定是：不自行實作推論引擎。**

vLLM 的 `OffloadingConnector` 已提供 GPU → CPU →（FS / 物件儲存 / P2P）的搬移機制，
官方文件明確載明支援 **CUDA、ROCm 與 XPU**，其上游 CI 於實體 gfx942 硬體上執行 KV offload 測試。
該連接器內建的置換策略**僅有 `lru` 與 `arc`**，並透過 **`CachePolicy` 介面**開放自訂實作。

三個後果：
1. **Tiara 可在 CUDA 與 ROCm 兩個平台上零程式碼修改部署**；
2. 對照組是 **production 級實作**而非自行撰寫的弱化版本；
3. **貢獻邊界清晰**——故 Tiara 是一個 **plugin 而非 fork**。

**這也界定了 baseline 的意義**：`lru` 與 `arc` 是 vLLM production 預設策略的**完整**集合，
而兩者**皆僅依存取歷史決策**：
> 一個「重算需 225 μs」的 block 與一個「自 CPU 取回需 4.2 μs」的 block，**在 LRU 眼中完全等價**。
> 本文的成本模型正是針對此空缺。

### 6.2 動作 $a_i$ 到底是什麼（圖 3）

**(a) 作用範圍是一整欄**：vLLM 的 block table **以 token 為索引且跨層共用**，
故一個 $a_i$ 同時套用於 **16 token 的全部 layer、全部 KV head 與 K/V 兩者**。
`layer_id` 與 `head_id` **僅作為預測器的特徵**，不切分動作空間。

**(b) 逐 block 而非全域**：任一時刻的垂直切片上**不同 block 位於不同狀態**
（圖 3b 示意：同一前綴的 6 個 block 在 6 個連續請求中，$r_4$ 時刻四種狀態並存）。

**(c) 合法轉移**：
```
GPU BF16 ──降級(虛線)──▶ GPU FP8 ──降級──▶ GPU INT4        ← 不可逆（丟掉的位元取不回來）
   ▲ │
   │ └──搬移(實線,無損,可逆)──▶ CPU ◀──▶ SSD ──丟棄──▶ DROP
   │
   └──────────── 重算（橘線，唯一回到 BF16 的路徑，成本 C⁰ + αp）──────────┘
```
**圖中沒有任何一條箭頭是「精度往上」。**

### 6.3 預測器的四個設計面（表 7 的比對結果）

| 系統 | 模型 | 動作數 | 成本不對稱 |
|---|---|---|---|
| KVP | 112×MLP | 2 | 否 |
| ForesightKV | MLP(16) | 2 | 部分（單邊） |
| LookaheadKV | LoRA $r{=}8$ | 2 | 否 |
| Marconi | 啟發式 | 2 | 否（但有 value/byte） |
| Mockingjay | 表格 + TD | 2 | 否 |
| LRB | GBDT | 2 | 否（L2） |
| **PARROT** | LSTM + attn | 2 | **是（NDCG gain）** |
| **Sibyl** | C51 DQN | $n$ | **是（實測延遲）** |
| **Tiara（本文）** | GBDT | **6** | **是（動作成本加權）** |

#### 為什麼選 GBDT 而非神經網路

**核心理由：預測器的成本自其收益中扣除。**
預測器運行於服務熱路徑，其延遲**直接抵銷**所避免的重算或傳輸。
以表 5 為參照：單次重算 12–1,972 μs，而對 64 個候選的 GBDT 推論為 **30 μs**；
若改以需要一次 forward pass 的模型評分，**僅 kernel launch 與排隊即進入相同數量級**，收益隨即被抵銷。

**第二個理由：神經預測器與主模型競爭同一加速器。**
GBDT 於 CPU 上執行，完全不佔 GPU；任何在 GPU 上評分的預測器都會**插入主模型的 decode 批次**,
其代價不僅是預測器本身的延遲，還包括**對批次結構的擾動**。

**這個權衡隨主模型增大而更有利於 GBDT**：$\kappa$ 隨模型增大而上升（8B→70B：6→21 於 MI300X、54→190 於平台 A），
而 GBDT 的推論成本**不隨模型規模改變**，因其特徵取自存取歷史而非模型參數。

**論文不要求讀者接受此論證，而以實驗回答**（表 15 的 (D) 區塊）：
前三列（GBDT、MLP $2\times64$、MLP $4\times256$）共用攤平特徵，檢驗**容量**；
第四列 GRU 直接消費 `deltas` 時間序列，檢驗**結構**。
> 此對照不可省略：**LRB 所比較的 2 層 16 隱藏單元 NN 同樣接受攤平輸入，
> 其「GBM 勝出」的結論因而不涵蓋序列模型**，本文若僅援引該結論即屬過度推論。
> **若 GRU 在計入自身延遲後仍佔優，§5.2 的整條特徵工程路線即被推翻。**

#### 標籤怎麼取得（兩個機制，缺一不可）

**標籤 $\tau_i$ 無法在記錄特徵的當下取得，因其定義涉及未來。**
決策是 $t_d$ 當下的一次前向流程（約 30 μs，執行於 CPU），但標籤要等一整個視窗。

樣本進入待標記佇列，由兩個機制之一完成標記：
- **(a)** 該 block **下次被存取**時，以兩次存取的間隔為標籤；
- **(b)** 當該樣本**離開一個長於 relaxed-Belady 邊界的滑動視窗 $W$ 而仍未被存取**時，立即以「$>W$」標記。

> **機制 (b) 是取得負樣本的唯一途徑。**
> 僅有 (a) 時，訓練集只會包含「後來確實被重用」的 block——從未被重用者永遠等不到第二次存取，
> 因而永遠不進入訓練集。**模型將完全未見過應被丟棄的樣本長何模樣，而那正是 `Drop` 動作的目標群體。**

取樣**以 block 為單位而非以請求為單位**，以避免偏向熱門 block。

#### 隨機切分會洩漏未來

訓練與測試集依 trace 的**時間順序**切分（前 70% 訓練、後 30% 測試）。
隨機切分會使訓練集含有測試期之後的存取事件，而 **EDC 與 delta 特徵本身即為時間的函數**，
此洩漏將**系統性高估**預測品質。（快取預測文獻中屬常識，但其違反不易由結果察覺，故明確記載。）

#### 分布飄移

不同工作負載的重用結構不同：多輪對話的前綴重複命中、RAG 的文件塊多為一次性讀取、
agent 流量呈現極熱的系統提示與極冷的工具輸出（SAECache 量到的 **756 倍**重用率差異即為直接證據）。

三個層次因應：
1. `token_type` 作為特徵，使模型在單一模型內區分語意類型；
2. **週期性重訓**——利用 GBDT **300 ms** 的訓練成本，以最近時窗的樣本重新擬合；
3. 報告**跨工作負載泛化**（於 A 訓練、於 B 測試的退化幅度）。

> 第 2 項是 GBDT 選擇的**直接後果而非附帶效果**：
> KVP 的 112 個 RL agent 需 8 GPU DDP 訓練，其重訓週期以**日**計，
> **結構上無法追隨以分鐘計的流量變化**。
> **對分布飄移的適應成本，是預測器族選擇的一部分，而非可事後補救的工程細節。**

### 6.4 推論開銷預算

既有工作給出三種攤銷方式：
- **KVP**：於 prefill 後評分一次、decode 期間零額外 FLOP（10K 上下文下 **0.71 ms**，相對於 404 ms 的 prefill）
- **ForesightKV**：每 **256** 個新 token 重新評分一次，開銷 **2.7%**
- **TRIM-KV**：於 block **建立時**評分一次，之後以指數衰減 $\beta^{t-i}$ 免費地重新排序

**本文無法直接採用 TRIM-KV 的做法**，因為 §5.2 的特徵（deltas 與 EDC）**在每次存取時更新**，
一次性評分會立即過時。改採**三層攤銷**：
1. 存取時以極低成本更新 EDC；
2. 於驅逐時機依 LRB 的做法**取樣 $k=64$ 個候選評分而非排序整個 cache**（CPU 上約 30 μs）；
3. 粗粒度的週期性全量重新評分。

> **Pythia**（線上 RL 硬體 prefetcher，僅佔 **1.03%** 晶片面積）提供了
> 「線上學習控制器能符合真實硬體預算」的存在性證據。

### 6.5 與 Sibyl 的差異：成本是否進入訓練

**Sibyl 是唯一處理異質層級放置且考慮成本的系統，但它迴避了此問題**：
其 reward 直接取自**實測延遲** $1/L_t$，**成本比是硬體量出來的而非猜的**。這是優雅的做法。

**但 Sibyl 的動作可逆**（頁面可在下次存取時重新放置），
而本文的 **`Drop` → 重算不可逆**，因此需要把 Sibyl 的驅逐懲罰項 $R_p = 0.001 \times L_e$
**推廣為 per-action 懲罰**。

### 6.6 硬體條件化的具體機制

$w(a_i)$ 由 FLOP/byte 比值決定，而該比值在 RTX 3090 為 2,254、MI300X 為 20,752。
**同一個損失函數在兩個平台上會產生不同的權重，進而學出不同的策略。**
§2.6 的硬體條件化主張在此獲得一個**具體且可量測的機制**，而非僅是敘述。

**須明確一項取捨**：
- **加權訓練**使模型**綁定於特定的 $\kappa$**，平台變更即需重訓 → 以可移植性換取邊界附近的精度
- **兩階段做法**（對稱訓練 + 事後移門檻）保持平台無關，但擬合能力的配置不隨決策邊界移動

**二者並非優劣關係而是不同的操作點**，表 15 的 (B) 區塊同時量測二者，
使該取捨成為**可報告的結果**而非未言明的假設。

### 6.7 刻意不回報分類準確度（附錄 B.6）

**這不是疏漏，是方向問題**：
成本敏感訓練的作用即是把決策邊界自 0.5 移至 $p^{*}$，
其必然後果是模型在**廉價方向**上多犯錯以換取在**昂貴方向**上少犯錯。
以未加權的準確度衡量，**成本敏感模型應當劣於對稱模型；若其未劣化，反而意味著加權未生效**。

改以兩個量診斷：
1. **校準品質**（可靠度圖 + expected calibration error）——
   式 (9) 的最佳門檻**僅在 $\hat p_i$ 為校準過的機率時成立**。
   此量同時是「(L1) 改變條件均值而非決策門檻」這個缺陷**是否被校準步驟補救的唯一直接證據**；
   **缺少此圖，該缺陷等同未被處理**。
2. **成本加權錯誤率** $\sum_i c(a_i^{\text{pred}}, a_i^{\star})$——
   計的是「**錯掉多少微秒**」而非「錯了幾次」，是唯一能在預測器層級公平比較兩種損失函數的指標。

### 6.8 服務層級分佈取代單一命中率

「KV hit rate」對單層快取有明確意義，但本文的動作空間有六階，**「命中」並非二元事件**：
自 CPU 取回與自 SSD 取回的成本差約一個數量級，與重算則差 6–190 倍。
**壓成單一比率會抹除本文所要量測的成本結構。**

改為回報**服務層級分佈**（每次 KV 存取由 $\mathcal{A}$ 中哪一階滿足的經驗分佈），另加兩個純量：
- **GPU 常駐率**：存取時該 block 已位於 GPU 且為 BF16 的比例（對應傳統命中率）
- **重算率**：由 `Drop` 觸發重算的存取比例（**本文特有**，直接對應 §2.4 的成本模型，
  亦是表 15「移除 `Drop`」一列的解讀依據）

---

## 7. 量測結果：七項發現與 go/no-go

**平台 A**：NVIDIA RTX 3090（24 GB GDDR6X、PCIe 4.0×16、sm_86、driver 550.163.01、CUDA 12.4），
vLLM 0.28.0 + PyTorch 2.13.0+cu129。**平台 B（MI300X）尚無機器**，其設計列於附錄 B。

**工作負載**：Mooncake 的 `toolagent`（23,608 請求）與 `conversation`（12,031 請求），
**中位長度 6,352 token、最長 126,208**。目標長度區間（65K–524K）**無公開 trace 可用**，
以合成長上下文補足，**其重用率與長度分布由作者設定**（此限制明確標註）。

> **量測衛生三條**（§A.1，該機器由二十餘位使用者共用）：
> 1. **每卡獨佔不足以保證乾淨**——GPU 的 SM 各卡獨佔，但 **PCIe、host RAM 頻寬與 `/dev/shm` 為全機共用**。
>    逐格比較序列與五卡並行的 80 個量測點：不經 PCIe 的 `full_gpu` 差異在 ±2% 內，
>    而**經 PCIe 的 warm TTFT 差異達 −26% 至 −52%**。**僅序列模式的數據進入論文。**
> 2. **KV pool 大小在同一設定下並非常數**——同模型、同卡、同 `gpu_memory_utilization`，
>    `GPU KV cache size` 在 5.09 與 5.88 GiB 間擺動（原始 token 數全距 13.5%）。
>    **以每 GiB 可容 token 數正規化後全距降至 0.04%**，故容量一律以 KiB/token 報告。
> 3. **分層量測須驗證資料確實落到目標層**——首次量測即因此失效：
>    CPU 階 24 GiB、工作集 8 GiB，磁碟階僅被查詢 **2** 次而 CPU 階 **2,048** 次，
>    所得「`SSD` 與 `CPU` 僅差 1.2%」**實為 CPU 階的數字**。縮至 1 GiB 後磁碟階被查詢 2,048 次。

### 7.1（§6.3）動作空間的有效子集由成本常數決定，可事先算出

**兩個判別式，都只需要成本常數，不需要試跑**：
- **精度階由硬體與後端支援決定**：四個 GPU 精度階皆為 `--kv-cache-dtype` 的取值，無須額外實作；
  **但可切換不等於可用**——三個低精度階中**僅 INT8 一階在檢索任務上保住品質**。
- **位置階由 $P^{*}$ 與請求長度的關係決定**：於 128K 上下文，落在 `Drop` 一側的 block 佔
  **8.3%（`llama-bf16`）至 28.8%（`qwen-awq`）**。

> **Takeaway**：六階動作空間是**候選**集合，不是每個平台上都成立的必要集合。
> **這不是本文主張的反例，而是其可操作形式。**

### 7.2（§6.4）Tier 0 baselines：長上下文下五者有四者失效

兩輪相同長前綴（cold = 無快取，warm = 量其保留能力），`qwen-awq`、4 個前綴：

| 對照組 | 機制 | cold (ms) | warm (ms) | 加速 |
|---|---|---|---|---|
| **ctx = 131,072** | | | | |
| `full_gpu` | 不卸載 | 99,681 | 100,152 | 1.0× |
| `cpu_arc` | CPU 階 + ARC | 102,702 | 72,883 | 1.4× |
| `tier_fs` | CPU + 磁碟兩階 | 101,518 | **13,626** | **7.5×** |
| `lmcache` | LMCache | 102,248 | 102,603 | 1.0× |
| **ctx = 258,048** | | | | |
| `full_gpu` | 不卸載 | 325,520 | 322,579 | 1.0× |
| `cpu_arc` | CPU 階 + ARC | 327,295 | 325,767 | 1.0× |
| `tier_fs` | CPU + 磁碟兩階 | 326,393 | **43,664** | **7.5×** |
| `lmcache` | LMCache | 333,046 | 335,034 | 1.0× |

**於 258K，五個系統中僅 `tier_fs` 保有效益**（省下 282.7 秒）。
CPU 階於 131K 尚有 1.4×，至 258K 即失效：**24 GiB 的 CPU 階可容 449,536 token，
而 4 個 258K 前綴的工作集為 1,032,192 token**。
（ctx 32K–65K 時五者相近 42–77×，因工作集仍可置入 GPU，卸載階未被使用。）

> **一項保留**：`lmcache` 於全部長度皆為 1.0×。此**可能為組態錯誤而非該系統的能力上限**；
> 在排除此可能之前，論文**不宣稱** LMCache 於長上下文無效。

### 7.3（§6.5）decode 佔總時間的四分之三，而放置決策對其無自由度

見式 (12)。**decode 期間該請求的 KV 必須整份駐留於 GPU**——每一步皆需讀取完整 KV，
**無搬移至 CPU/SSD 或丟棄重算的餘地**。

以 Mooncake 的真實 `output_length`（toolagent 中位 30、p90 507；conversation 中位 350、p90 597）換算：
decode 佔端到端 **48.8%**（toolagent）與 **53.8%**（conversation）；
**於 `llama-bf16` 剖面升至 75%**。
→ **端到端 headroom 約為 prefill headroom 的 0.88 倍。**

### 7.4（§6.6）品質約束的作用範圍

**無損動作：$\epsilon = 0$ 是可驗證的斷言**（不是品質取捨，是**正確性**斷言——輸出有異即為實作錯誤）。
以 64-shot GSM8K（共用前綴 11,029 token）對 60 題取輸出的 SHA-1：

| 設定 | 正確率 | 逐字元相同 |
|---|---|---|
| `full_gpu`（基準） | 76.67% | — |
| `cpu_lru` | 76.67% | **60/60** |
| `tier_fs`（CPU + 磁碟） | 76.67% | **60/60** |

**有損動作：退化由任務決定，不由精度決定**（表格見 §2.6(c)）。

**四個關鍵子發現**：
1. **僅以推理任務評測會得出「KV 量化幾無代價」的錯誤結論。**
   GSM8K 上四者的差異均與零無法區分（$n=1,000$ 時 95% CI 為 $\pm3.7$ pp）；
   同一組設定於檢索任務上由 100% 降至 0%。
   （功效分析：區分 2.8 pp 需每設定 $n \approx 1,760$，7 個 $f$ 值即 12,320 次請求，
   **而 2.8 pp 已是 $f=1$ 的上界效果** → GSM8K 無法用於量測 $\epsilon(f)$ 曲線。）
2. **真實文件上的長上下文理解與檢索同側，不與推理同側。**
   LongBench 的 **7 個英文任務無一例外地站在檢索那一側**（巨觀平均 58.4 → FP8 的 6.6 與 INT4 的 0.4）。
   **摘要（GovReport, ROUGE-L）與多跳 QA（HotpotQA, 2WikiMQA, F1）——兩個不需要逐字回收任何字串的任務
   ——掉幅與純檢索同量級**（FP8 −51.8 pp，95% bootstrap CI $[47.3, 56.3]$；INT4 −58.0 $[53.7, 62.3]$）。
3. **「哪個精度安全」的答案取決於評測選了哪個 benchmark。**
   INT8 在前三項上都讀起來無損（GSM8K −1.5 n.s.、LongBench −1.3 $[0.0, 2.8]$、大海撈針 −5），
   但於 RULER 巨觀平均 79.1 vs 91.6，配對差值 **12.5 $[8.6, 16.7]$——與零可區分**。
   差距幾乎全部來自單一任務 **`niah_multikey_3`**（鍵與值皆為 UUID、整片 haystack 由同構干擾針構成），
   **INT8 於此只剩 53.3**（配對差值 46.7 $[30.0, 63.3]$）。
   → **同一個精度階在四個評測上分別是「無損」「無損」「幾乎無損」與「掉一半」。**
4. **梯度由「答案是否為某個確切位置的確切 token」決定。**
   - **答案是全域統計量者最耐受**：FWE（Zipf 分佈下最高頻的三個詞）是 14 個任務中
     **唯一 FP8 未全失者**（21.1），亦是**唯一 INT8 未掉者**（87.8 vs 85.6，差值 −2.2 $[-6.7, 2.2]$）
   - 需要聚合但必須逐項列出者次之：CWE（前 10 高頻詞）掉 12.0 $[4.7, 20.3]$
   - 多跳追蹤 VT 掉 6.7 $[-2.0, 16.0]$
   - **四個 NIAH 變體（答案是某個確切位置的確切字串）掉 6.7 至 46.7**

   → **KV 量化的雜訊會抹掉逐 token 的位置證據，卻不會抹掉整份上下文的統計形狀。**

**逐任務分數**（表 13，`qwen-awq`、四個設定吃**同一批 prompt**、`temperature`=0、固定 seed，
唯一變動的是 `--kv-cache-dtype`）：

| 任務 | 類別 | $n$ | BF16 | FP8 | INT8 | INT4 |
|---|---|---|---|---|---|---|
| **LongBench（真實文件、32K 視窗）** | | | | | | |
| MultiFieldQA | 單文件 QA | 50 | 51.5 | 7.4 | 51.3 | 0.2 |
| Qasper | 單文件 QA | 50 | 41.4 | 2.8 | 40.8 | 0.5 |
| HotpotQA | 多跳 QA | 50 | 53.4 | 2.2 | 54.1 | 0.0 |
| 2WikiMQA | 多跳 QA | 50 | 52.2 | 7.5 | 50.9 | 0.0 |
| GovReport | 摘要 | 50 | 34.0 | 2.1 | 34.1 | 1.5 |
| TREC | 分類 | 50 | 76.0 | 22.0 | 74.0 | 0.0 |
| PassageRetr. | 合成檢索 | 50 | 100.0 | 2.0 | 94.0 | 0.3 |
| **巨觀平均** | | | **58.4** | **6.6** | **57.0** | **0.4** |
| **大海撈針（合成、單鍵）** | | | | | | |
| 4K / 8K / 16K / 32K | 單鍵檢索 | 20 each | 100 / 100 / 100 / 100 | 5 / 0 / 5 / 5 | 95 / 90 / 90 / 95 | 0 / 0 / 0 / 0 |
| **RULER（合成、16K）** | | | | | | |
| NIAH multikey-2 | 多鍵檢索 | 30 | 100.0 | 0.0 | 90.0 | 0.0 |
| **NIAH multikey-3** | **多鍵（UUID）** | 30 | 100.0 | 0.0 | **53.3** | 0.0 |
| NIAH multivalue | 一鍵四值 | 30 | 100.0 | 0.0 | 93.3 | 0.0 |
| NIAH multiquery | 四鍵查詢 | 30 | 100.0 | 0.0 | 92.5 | 0.0 |
| VT | 多跳追蹤 | 30 | 83.3 | 1.3 | 76.7 | 0.0 |
| CWE | 詞頻（前 10） | 30 | 72.3 | 0.7 | 60.3 | 0.0 |
| **FWE** | **詞頻（前 3）** | 30 | 85.6 | **21.1** | **87.8** | 0.0 |
| **巨觀平均** | | | **91.6** | **3.3** | **79.1** | **0.0** |

**FP8 於這 14 個任務上最高只到 22.0、INT4 最高 1.5：表中的零是量到的零，不是缺資料。**

> **三項方法學細節**（§A.5）：
> - **信賴區間以配對 bootstrap 求得**（四個設定評同一批題目，故差值逐題相減後再重抽題目，10,000 次）。
>   **配對消去題目難度的變異；未配對的版本寬約三倍。**
> - **絕對值不可與公開榜單並列**（模型是 AWQ-INT4 且移除 DCA 的變體、每任務取前 50 筆、32K 視窗）。
>   **但四個設定吃的是同一批 prompt，故跨精度的相對變化不受影響——本文宣稱的正是相對變化。**
> - **計分函式已對上游逐筆驗證**：英文四項指標為重寫（避免把 `jieba` 拉進推論環境），
>   以 9,500 組隨機字串與真實預測對上游逐筆比對，容差 $10^{-9}$ 下**零筆不一致**。

### 7.5（§6.7）寫入頻寬是磁碟階的可行性門檻

見 §2.6(f)。**兩個剖面的可部署性結論不同**：
- `llama-bf16`：`tier_fs` 既是最佳 baseline，又需要 4,666–4,892 MiB/s，
  **超出唯一做過持續寫入量測的裝置（SATA QLC，181 MiB/s）26×**，亦超出 NVMe 短測上界 2,512 MiB/s。
  即使以「每個不重複 block 僅寫一次」的下界計算仍需 3,086–3,208 MiB/s，
  **故此結論不依賴 load-and-evict 的建模選擇**。
- `qwen-awq`（主設定）：最佳 baseline 為 `cpu_arc`，**它完全不使用磁碟階**；
  `tier_fs` 的需求降至 2,016–2,122 MiB/s，下界 1,350–1,404 MiB/s。

### 7.6（§6.8）headroom 的峰值出現在成本交叉點的 2–3.5 倍處

見式 (13)。固定記憶體壓力 $5\times$、重用率 80.9%，僅改變請求長度
（GPU 預算隨工作集等比放大），`qwen-awq` 的 $P^{*} = 37,717$：

| 請求長度 | headroom | 判定 |
|---|---|---|
| 32K | 8.94% | MARGINAL |
| **65K–262K** | **24.44%–33.50%** | **三者皆 GO** |
| 524K | 12.71% | MARGINAL |

**峰值落在 $3.5\,P^{*}$ 處。** 圖 5 另有三條重用率 59%–89% 的細線，
**四條線的峰值皆落在 $2$–$3.5\,P^{*}$ 的區間內**
→ **峰值位置由成本結構決定，而非由重用率決定。**

> 524K 一列有兩項限制：單請求 KV 超出 GPU 預算，
> 且該長度的上半段（位置 258,048 以上）**超出 `qwen-awq` 成本模型的擬合上限，為線性外插**。

**峰值位置隨硬體移動**（圖 6）：以實測 $\alpha$ 按算力倍率縮放模擬，
峰值自平台 A 的 **262K** 移至 6 倍算力下的 **524K**，峰值 headroom 自 **18.7%** 升至 **39.5%**。
**虛線係推算而非量測。**

**於 MI300X 的預測**（$1,307.4/71.2 = 18.4\times$）：$P^{*}$ 為 346K–693K，
甜蜜點落在 **692K–2.4M token**。**1M 級的單請求上下文正落於此區間之內**，
而 Llama-3.1-8B 於 1M 需 122 GB KV，**單張 MI300X 可容納**
→ **此預測在一張卡上就能證實或推翻，不需要多租戶設定。**

同一組數字也界定了生產工作負載的位置：**Mooncake 的最長請求 126,208 token 僅為該峰值的 1/10 至 1/19**，
故**今日的生產流量在此類硬體上量不到放置決策的價值**——
這不是方法的缺陷，而是 $P^{*}$ 隨算力右移的直接推論。

> **可證偽性**：$\alpha$ 的縮放假設 prefill 時間與峰值算力成反比，
> 忽略了 MFU 隨模型與序列長度的變化、kernel 實作差異、以及 ROCm 與 CUDA 的成熟度落差。
> **驗證方式明確**：於目標加速器上重量四項成本常數（`Cpu`、`Ssd`、$C^0_{\text{recompute}}$、$\alpha$），
> 代入式 (13) 求 $P^{*}$，再掃描長度。**若峰值不落在 $2$–$3.5\,P^{*}$，該機制解釋即被推翻。**

### 7.7（§6.9）權重精度決定 24 GB 級加速器能否進入長上下文區間

| 設定 | 單卡 KV 容量（token） |
|---|---|
| Llama-8B **BF16 權重** | **41,648** |
| Llama-8B AWQ 權重 | 120,320 |
| Llama-8B AWQ + FP8-KV | 240,656 |
| Qwen-7B-1M AWQ 權重 | 273,872 |
| Qwen-7B-1M **AWQ + FP8-KV** | **547,744** |
| DeepSeek-V2-Lite **MLA** AWQ | **399,376** |

BF16 權重在 24 GB 卡上佔 15 GB，KV 預算僅餘 5.9 GB → **無法進入 128K 區間**。
AWQ-INT4 權重佔 4.7 GB，KV 預算增至約 15 GB。**相差 $13.2\times$。**

**兩個後果**：
1. **平台 A 足以驗證 128K 區間的容量與成本主張，無須更大的加速器。**
2. **MLA 架構把 KV 壓至 30.4 KiB/token（GQA 的 $1/4.2$）**，
   使單卡容量達 399,376 token——**在同一張 24 GB 卡上把壓力軸往後推了 $4.2\times$，
   與換一張大卡的效果同向**。
   → **這使 MLA 成為 $\kappa$ 軸上的第三個資料點，且不需要第二台機器。**

### 7.8（§6.10）go/no-go 判定：條件式 GO

**停止條件（訂於任何策略實作之前）**：
> 若 oracle 相對於最佳**可部署**策略的改善幅度**小於 5%**，終止此研究方向並報告此負面結果。

| 剖面 | 工作負載 | 計時 | headroom | 對停止條件 |
|---|---|---|---|---|
| `qwen-awq` | toolagent | 端到端 | 8.42% | 不觸發 |
| `qwen-awq` | conversation | 端到端 | 9.23% | 不觸發 |
| `llama-bf16` | toolagent | 端到端 | 2.83% | **觸發** |
| `llama-bf16` | conversation | 端到端 | 3.34% | **觸發** |
| `qwen-awq` | 65K–131K | prefill | **31.8–33.5%** | 不觸發 |

**判定：條件式 GO。** 兩個條件各有已量到的機制，且**皆可由成本常數事先算出，無須試跑**：

1. **權重須量化**——BF16 權重使 decode 佔端到端時間的 **75%**，而放置決策對 decode 無自由度（§7.3），
   故 `llama-bf16` 落在停止條件之下；AWQ-INT4 使 decode 的固定成本減半，headroom 升至 8–9%。
2. **請求長度須落在 $2$–$3.5\,P^{*}$**——Mooncake 的中位長度 6,352 token **僅為 $P^{*}$ 的 0.17 倍**，
   遠在甜蜜點之外；於 65K–131K，同一剖面的 prefill headroom 為 31.8%–33.5%。

> **表中五列相差 $12\times$**，因此 headroom 是一條**隨剖面與長度變動的曲線**，
> **取其中任一列為代表都會誤導**；論文引用時一律標明設定。
>
> **三處尚無數據**：(i) 線上策略能取得 oracle 的多少比例；(ii) 平台 B 的常數；
> (iii) 末列 65K–131K 為**合成**長上下文，其重用率與長度分布由作者設定。
> **目前沒有公開 trace 同時具備長上下文與真實的低重用率——這是本文最實質的證據缺口。**

---

## 8. 引用文獻核心：每一篇在講什麼

> 依論文 §3 的家族分類。每篇給三件事：**做什麼**、**核心機制**、**在本文裡扮演什麼角色**。
> 標 `[PREPRINT]` 者為未經同儕審查之 arXiv 預印本（`refs.bib` 已逐條標註）。

### 8.1 分層儲存（GPU / CPU / SSD）

| 工作 | 場所 | 做什麼 |
|---|---|---|
| **FlexGen** | ICML'23 | **首次以線性規劃決定 KV 在 GPU/CPU/disk 三層間的比例配置。** 單 GPU 高吞吐生成，把權重、KV、activation 三者一起排進 LP。**限制：策略為離線靜態**——配置一次算好，不隨執行期變動。在特徵矩陣裡它有 GPU/CPU、SSD、壓縮、恢復，但**沒有未來預測、沒有重算、沒有品質約束**。 |
| **CachedAttention** | USENIX ATC'24 | **針對多輪對話保留 session 級 KV** 於 HBM/DRAM/SSD 三層，並**利用排程器佇列進行預取**（知道哪個 session 即將被排到，就先把它的 KV 拉上來）。 |
| **Mooncake** | FAST'25（Best Paper） | **KVCache-centric 的分離式架構**：把叢集中閒置的 CPU/DRAM/SSD **池化為共享 KVCache 層**。**本文的生產 trace 即取自此**（`toolagent` 23,608 + `conversation` 12,031 請求，中位長度 6,352 token）。⚠️ 其 `hash_ids` 是 **512-token** 的 block（不是 16-token），單位弄錯會使工作集少算 32 倍。 |
| **KVDrive** | preprint'26 | **以 prefill 階段的 attention 重要性剖析**決定 HBM/DRAM/SSD 的**初始**配置，並**將全量 KV 寫入 SSD 作為後備**。 |
| **Tutti** | preprint'26 | **專注於 SSD I/O 路徑本身**：透過 **GPU 發起的儲存存取**消除 CPU 瓶頸。 |
| **MTDS** | Complex & Intelligent Systems'26 | 資源受限條件下的多階動態 KV 儲存。是少數同時具備 SSD、恢復與**重算**的既有工作。 |

**這一族共同的限制**：**放置訊號來自當前或過去的觀測，且不將重算視為動作。**

### 8.2 可恢復驅逐（被降級者能召回）

| 工作 | 場所 | 做什麼 |
|---|---|---|
| **ArkVale** | NeurIPS'24 | 觀察到 **token 重要性隨解碼步驟變動，早期被驅逐者可能重新變得重要**。以 **page digest**（key 向量的**包絡體**）估計已驅逐頁的重要性並**召回**。做的是**位置分層**（GPU↔CPU，兩層皆全精度）。 |
| **QEvict** | preprint'26 | 把可恢復性建立在**精度**維度：**中等信心的視窗被量化保存，重新變重要時反量化升回全精度。** 做的是**精度分層**（全精度↔量化，但僅一個位置）。 |

> **本文的切入點**：這兩類工作**分居兩個正交維度**。
> **據作者所知，尚無工作以單一重要性驅動策略統一管理兩者**——這正是式 (4) 六階階梯要做的事。

### 8.3 學習式未來效用預測（2026 年的新一波）

#### KVP（ICML'26, Apple，"Learning to Evict from Key-Value Cache"）— **使用者特別問的那篇**

- **做什麼**：以**強化學習代理排序 token**，決定驅逐哪些 KV。
- **核心機制**：**per-head** 的 RL agent，其 **reward 直接導自未來效用**
  （不是手寫啟發式，而是從「這個 token 後來到底有沒有用」學回來）。
  對 Qwen2.5-7B-Instruct 需為 **112 個 layer-head 各訓練一個 agent**（$112 = $ 層數 × head 數），
  其 README 載明使用 **8 GPU DDP** 訓練。
  **明確「僅使用 key 與 value 向量」作為輸入。**
- **在本文裡的角色**（出現 7 次，是被最頻繁對照的工作）：
  1. **表 7 的第一列**：112×MLP、**動作數 2**、**成本不對稱：否** → 本文「對稱損失失準」主張的直接對象。
  2. **反對 per-head 分模型的論據**：本文採單一全域 GBDT 並以 layer/head id 作為特徵，
     理由是「**KVP 的 112 個獨立 agent 帶來可觀的工程成本，而其論文並未提供
     『per-head 模型優於帶 head-id 特徵的共享模型』的消融證據**」。
  3. **分布飄移論證的反例**：KVP 的 112 個 RL agent **重訓週期以日計，
     結構上無法追隨以分鐘計的流量變化** → 論證「對飄移的適應成本是預測器族選擇的一部分」。
  4. **推論開銷的正面參照**：於 prefill 後評分一次、decode 期間零額外 FLOP
     （10K 上下文下 **0.71 ms**，相對於 404 ms 的 prefill）。
  5. **「不看 KV 而預測 KV 站不住腳」的三個先例之一** → 促使本文把 pooled KV 統計併入特徵集。
  6. **無法重跑的對照組**：**未釋出訓練後的權重**，且預測器**模型專屬**
     → 本文因此**不以外部學習式系統作為核心主張的檢驗手段**，改用表 15 (B) 區塊的同系統換損失。

#### ForesightKV（ICML'26）

- **做什麼**：為**推理模型**最佳化 KV 驅逐，學習 token 的**長期貢獻**。
- **核心機制**：建構以**真實未來 attention 分數**計算的 **golden eviction oracle**，
  再以**模仿學習**逼近它。模型是 MLP(16)。**每 256 個新 token 重新評分一次，開銷 2.7%。**
- **在本文裡的角色**：表 7 中**唯一「部分（單邊）」成本不對稱**者；
  「不看 KV 而預測 KV」的三個先例之一（它**蒸餾真實的未來 attention**）；
  無法重跑（未釋出權重，且**需至少兩張 GPU 分置訓練模型與參考模型**）。

#### LookaheadKV（ICLR'26, Samsung）

- **做什麼**：**不做實際生成**，就「瞥見未來」來驅逐 KV。
- **核心機制**：以**可學習的 lookahead token** 預測「模型真實回應所導出的重要性」。
  實作為層內的 **LoRA $r{=}8$** adapter。
- **在本文裡的角色**：表 7 中動作數 2、成本不對稱「否」；
  「不看 KV 而預測 KV」的三個先例之一（**透過層內 adapter 讀取**）；
  無法重跑（未釋出權重，**僅提供 minimal training example**）。

#### Marconi（MLSys'25，Outstanding Paper Honorable Mention）

- **做什麼**：**hybrid LLM（SSM + attention）時代的 prefix caching。**
- **核心機制**：效用函數**同時包含預測重用機率、計算節省與記憶體佔用**。
- **在本文裡的角色**：**特徵矩陣中覆蓋最高的既有工作（四項成本中的三項）**，
  也因此是本文貢獻宣稱最需要防守的對手。
  論文的回應是結構性的：**Marconi 僅在 GPU 上運作，故結構性地不可能具備傳輸成本。**
  另外，**hybrid SSM（如 Jamba）的狀態大小不隨序列成長，已由 Marconi 處理** →
  本文明確將其列為「**定義上不同的問題**」而非「未評估」。

> **這一族共同的限制**（本文最鋒利的一句）：
> 這些工作的動作空間**均為單層的保留/丟棄**，且**無一支援恢復**。
> **「最強的未來預測器沒有安全網，而最完善的安全網沒有未來預測器。」**

### 8.4 重算與傳輸的取捨

| 工作 | 場所 | 核心機制 |
|---|---|---|
| **Cake**（"Compute Or Load KV Cache? Why Not Both?"） | ICML'25 | **讓 GPU 正向計算與 I/O 反向載入同時進行並在中點會合**——不是二選一，而是兩端一起做。 |
| **KVPR** | ACL Findings'25 | **I/O-aware 的部分重算**：以 profiler + scheduler 求解**活化值傳輸與 KV 重算的最佳切分**。**ACL'26 綜述中「GPU KV 重算 ↔ KV 傳輸」重疊的表格，只列出這一個範例。** |
| **HCache** | EuroSys'25 | **儲存中間 hidden state（約為 KV 的一半大小）並由其重建 KV**，實質上在階梯上增加了第三階。**在本文裡另有一個關鍵角色**：由 hidden state 可**單步**重建 KV，從而**切斷式 (6) 的依賴鏈**。 |
| **CacheBlend** | EuroSys'25（Best Paper） | RAG 場景：把多個已快取 chunk 的 KV **融合**，只選擇性重算一小部分 token。代表「以 vLLM 為基礎、具備跨請求重用」的那條血脈。 |

> **這一族共同的時序特性**：**檢索時的反應式決策**——
> 它們回答「**此刻**需要這段 KV，如何最快取得」，
> 而非「**未來可能**需要，現在應置於何處」。**本文處理的是後者。**

### 8.5 品質作為約束

| 工作 | 場所 | 核心 |
|---|---|---|
| **KVServe** | SIGCOMM'26 | 把壓縮 profile 選擇形式化為 $\arg\min_p T_p(c)$ s.t. $T_p(c)\le T_{\text{SLO}} \wedge q_p(w)\ge q_{\min}$。**本文所知唯一在 Tier-1 場所將模型品質寫為硬性約束的 KV 工作**，**但其決策變數不含放置**。其 $\epsilon$ 可操作性做法（離線 profiling + $\epsilon$-greedy bandit）也是本文規劃的參照。 |
| **EvicPress** | preprint'25 | **聯合 KV 壓縮與驅逐**。品質**納入效用函數，但作為可調權重而非約束**。特徵矩陣中記憶體成本的代表。 |
| **AdaptCache** | SOSP BigMem workshop'25 | KV cache 原生的儲存階層，兼顧低延遲與高品質。同樣是**權重而非約束**。 |
| **LeoAM** | preprint'25 | 單張商用 GPU 上的自適應 KV 管理。**本文調查中唯一給出數值上界者**（宣稱準確度下降小於 1%）。 |
| **OrbitFlow** | VLDB'26 | 以 **ILP** 在延遲 SLO 與記憶體容量約束下決定**逐層放置**。**但因其為無損設計，ILP 中不存在品質變數。** |

### 8.6 形式化與上界

**Het-Mem / Fang 等人**（IEEE Computer Architecture Letters'25）
——**本文自我定位最直接的參照**。

- **做什麼**：將**異質記憶體系統中的 KV 放置問題數學形式化並推導理論上界**（HBM + off-package DRAM 兩層）。
- **關鍵**：明確指出「**存在可觀的執行期最佳化空間**」，但**同樣明確表示不提出具體排程策略**。
- **在本文裡的角色**：**「本文正是填補其留下的策略空缺。」**
  §6.2 的 oracle 在兩點上延伸之：**納入 SSD 層**，以及**納入 `Drop` → 重算動作**。

### 8.7 以 query 為訊號的稀疏檢索（本文論證「不能取代放置決策」的對象）

| 工作 | 場所 | 核心機制 |
|---|---|---|
| **Quest** | ICML'24 | **query-aware sparsity**：以當下 query 向量估計各 page 的重要性上界，取 top-$k$。 |
| **InfiniGen** | OSDI'24 | 動態 KV 管理：**推測哪些 KV 條目重要**（用前一層的輸入做部分 attention 預演），**只把那些從 CPU 預取回來**。 |
| **ShadowKV** | ICML'25 | **低秩 key cache 留在 GPU、value cache 卸載到 CPU**，執行時做精確的 KV 選擇。 |
| （ArkVale 亦屬此族） | | |

**本文對此族的兩個結構性反駁**（§3.4，很重要）：

1. **query 只覆蓋當下這一步。** $q\cdot k$ 回答的是「此刻這個 query 需要哪些 KV」。
   **跨請求的重用**——一段前綴在數分鐘後被另一個**尚未抵達**的請求命中——
   所對應的 query **尚不存在**，因而無法由此類訊號涵蓋。
2. **計算 $q\cdot k$ 預設 $k$ 仍可取得。** 要以 query 判斷是否保留某 block，必須先**讀取該 block 的 key**；
   **而是否保留該 key 正是待決策的變數。**
   此依賴使 query 訊號**無法用於已降級至 SSD 或已丟棄的 block**，
   亦即**無法用於階梯下緣——而階梯下緣正是成本差異最大之處**。

> **但本文不將二者視為競爭關係**：query 側訊號可作為**特徵**併入前瞻式預測器
> （演算法 1 第 5 行的 `attn_mass`），對仍可取得的 block 提供高品質輸入。

### 8.8 KV 量化（決定階梯上「精度那三階」該長什麼樣）

| 工作 | 場所 | 核心 |
|---|---|---|
| **KIVI** | ICML'24 | **免調參的非對稱 2-bit 量化**：**key cache 沿 channel 維度量化、value cache 沿 token 維度量化**，因二者**離群值結構不同**。→ 本文據此說明「階梯是示意，量化器可替換」。 |
| **KVTuner** | ICML'25 | **敏感度感知的逐層混合精度**：在 Llama-3.1-8B-Instruct 上達成 **3.25 位元**的近無損配置。 |
| **Hariri 等人**（"Quantize What Counts: More for Keys, Less for Values"） | preprint'25 | **在固定記憶體預算下，把位元優先配置給 key（如 4-bit K + 2-bit V）較均勻配置嚴格更優。** |

> **本文承認的範圍限制（§7 Limitations）**：本文**將 K 與 V 綁定於同一動作**
> （一同降級、一同量化、一同丟棄）。這是刻意的範圍限制，而**文獻指出該粒度並非最佳**。
> 故 `Gpu₈` 與 `Gpu₄` 應理解為 **K/V 共用精度的保守設定，其品質退化為 K/V 分離配置的上界**。
> 拆為兩軸會使 $|\mathcal{A}|$ 自 **6 增至 36**，並使標籤生成與 oracle 求解成本相應上升。
> 但**它與核心主張正交**——動作粒度變細只會使成本的離散程度更高，**強化而非削弱**該論點。

### 8.9 經典學習式快取（本文預測器設計的直接來源）

#### LRB — Learning Relaxed Belady（NSDI'20）— **本文預測器的藍本**

- **做什麼**：CDN 快取的學習式置換。
- **核心機制**（本文幾乎逐項沿用）：
  - **GBDT 迴歸 $\log(\text{距下次存取的時間})$**，而非二元分類
    （曾嘗試對 Belady 邊界做二元分類，並依 **good-decision-ratio 拒絕之**）
  - 三族特徵：**deltas**（前 $k$ 次存取間隔，涵蓋 LRU/LRU-$K$/S4LRU）、
    **EDC**（10 個指數衰減計數器）、**靜態特徵**
  - **取樣 $k=64$ 個候選評分而非排序整個 cache**
  - **明確比較過 GBM、線性迴歸、邏輯迴歸、SVM 與 2 層 16 隱藏單元的 NN，GBM 穩定勝出**
  - **訓練僅需 300 ms、對 64 個候選推論僅需 30 μs**
- **本文的誠實補充**：**LRB 比較的 NN 同樣接受攤平輸入，其結論不涵蓋序列模型**
  → 表 15 (D) 區塊加入 GRU 一列。

#### Mockingjay — Effective Mimicry of Belady's MIN（HPCA'22）

- **核心**：以**表格 + TD 更新**預測**連續的重用距離**（ETR），模仿 Belady 的 MIN。
- **在本文裡的角色**：與 LRB 並列為「**預測連續量並延後閾值化，勝過其二元分類前身**」的證據。

#### PARROT — An Imitation Learning Approach for Cache Replacement（ICML'20）

- **核心**：**模仿學習** + LSTM + attention 做快取置換。
- **關鍵**：**本文調查中唯一刻意使用非對稱損失的工作。**
  其 **NDCG** 形式明確「**重罰把機率放在即將被重用的 line 上，輕罰放在遠期重用的 line 上**」，
  帶來 **3.5%** 命中率提升。
- **本文的延伸**：**PARROT 只有單一動作，其 gain 項 $(d(l_w)-1)$ 只編碼重用距離，未編碼動作成本。**
  → 式 (11) 把 $\mathrm{cost}_{\text{err}}(a_w)$ 乘進 gain 項。

#### Sibyl（ISCA'22）— **最接近本文的既有系統**

- **做什麼**：**混合儲存系統中的線上強化學習資料放置**（不是 LLM，是 SSD+HDD 那種階層）。
- **核心機制**：**C51 DQN**，**$n$ 個動作**（非二元），
  **reward 直接取自實測延遲 $1/L_t$——成本比是硬體量出來的而非猜的**。
  另有驅逐懲罰項 $R_p = 0.001 \times L_e$。
- **在本文裡的三個角色**：
  1. 表 7 中**唯一同時具備「$n$ 個動作」與「成本不對稱：是」**者；
  2. **本文與之的差異**：**Sibyl 的動作可逆**（頁面可在下次存取時重新放置），
     而本文的 **`Drop` → 重算不可逆** → 需把 $R_p$ 推廣為 **per-action 懲罰**；
  3. **評估傳統的參照**：Sibyl 報告「**達到全知 oracle 的 80%**」，
     本文據此把 oracle 定位為**判準而非成績**，並在 §7 換算出「生產 trace 上 6.7–7.4%、
     甜蜜點 25–27%」的實際改善估計（**明確標註取自單一先前系統的達成率，不是本文的量測**）。

#### Belady（IBM Systems Journal, 1966）

- **MIN / OPT**：逐出「**下次使用最遠**」的項目。**單階時可證明最優。**
- **在本文裡**：演算法 2 第 11 行，**oracle 的 GPU 階逐出規則**。

#### Elkan — The Foundations of Cost-Sensitive Learning（IJCAI'01）

- **核心**：成本敏感學習的基礎結果，**最佳門檻 $p^{*} = c_{\text{FP}}/(c_{\text{FP}}+c_{\text{FN}})$**。
- **在本文裡**：**式 (9) 的出處**。論文明說「**我們不主張其為本文貢獻**」——
  貢獻是指出「既有 KV 工作因動作二元且成本同質而 $\kappa=1$，本文因動作異質而 $\kappa \ne 1$」。
  Elkan 的結果同時給出本文最重要的**基線**（對稱訓練 + 事後移門檻）。

#### Pythia（MICRO'21）

- **核心**：**線上強化學習的硬體 prefetcher**，僅佔 **1.03% 的晶片面積**。
- **在本文裡**：**線上學習控制器能否符合真實硬體預算的存在性證據。**

### 8.10 服務系統

| 工作 | 場所 | 在本文裡的角色 |
|---|---|---|
| **vLLM / PagedAttention** | SOSP'23 | **本文的實作載體。** block table **以 token 為索引且跨層共用** → 決定了動作 $a_i$ 的作用範圍是「16 token 的整欄」。`OffloadingConnector` 提供機制，`CachePolicy` 介面提供插入點，內建策略**僅有 `lru` 與 `arc`**。 |
| **LMCache** | preprint'25 | 企業級 KV cache 層，具分層後端。本文的 baseline 之一（**量到全長度 1.0×，但論文明言此可能為組態錯誤**）。其預設 `turboquant_k8v4`（K 8bit / V 4bit）是「key 比 value 敏感」的生產證據。 |

### 8.11 量測研究與綜述

| 工作 | 場所 | 核心 |
|---|---|---|
| **Bottlenecks**（KV offloading 瓶頸研究） | MLSys'26 | 量到在 80 GB 級硬體上 **99% 的延遲花在傳輸**。**但本文指出該結論建立在「容量壓力存在」的前提上**——在 MI300X 上該前提不成立。 |
| **Yandex Research**（KV Cache Offloading for Context-Intensive Tasks） | preprint'26 | **ShadowKV 等方法在 needle-in-a-haystack 上表現良好卻在多事實抽取任務上失效。** → 本文據此**把 needle-in-a-haystack 降級為 sanity check**，並以式 (14) 的 **min over tasks** 取代跨任務平均。 |
| **SAECache** | preprint'26 | **不同語意類型（system prompt / 使用者查詢 / 工具輸出 / 推理鏈）的重用率差異達 756 倍。** → 本文 `token_type` 特徵的唯一依據，且該特徵**取得成本為零**。 |
| **TRIM-KV** | preprint'25 | 於 **block 建立時評分一次**，之後以指數衰減 $\beta^{t-i}$ 免費重新排序。本文**無法直接採用**（因 deltas/EDC 每次存取都更新，一次性評分會立即過時）。 |
| **KV Survey** | ACL Findings'26 | **本文缺口論證的外部佐證**：其 **Observation O7** 指出壓縮與放置的協同設計是「**a missed opportunity**」，**Challenge C5** 呼籲在共享預算下聯合最佳化 eviction / offload / prefetch——**而該清單未包含重算**。其「重算 ↔ 傳輸」重疊表格**僅列出 KVPR 一個範例**。 |

### 8.12 硬體規格來源

| 來源 | 用途 |
|---|---|
| **AMD CDNA 3 白皮書** | MI300X 的 HBM 頻寬、PCIe 規格；MI300A APU 的記憶體架構 |
| **MI300X 效能分析**（Celestial AI, preprint'25） | 表 4 的 MI300X 實際規格數字 |
| **NVIDIA GA102 白皮書** | RTX 3090 的 **71.2 TFLOPS 推導**：328 TC × 1,695 MHz × 128 FLOP/clk（**FP32 累加**） |
| **UPM**（Dissecting CPU-GPU Unified Physical Memory on MI300A, preprint'25） | 支撐「**APU 上階梯會塌縮**」的限制討論——CPU 與 GPU 共用同一塊 HBM 時，「卸載至 CPU 記憶體」**僅是同一記憶體內的頁表變更，不產生容量收益**，`Cpu` 一階須重新標價。 |

### 8.13 特徵矩陣：一張表看完缺口（表 6）

| 工作 | 場所 | GPU/CPU | SSD | 未來預測 | 壓縮 | 恢復 | **重算** | **品質約束** |
|---|---|---|---|---|---|---|---|---|
| FlexGen | ICML'23 | ● | ● | | ● | ● | | |
| InfiniGen | OSDI'24 | ● | | ○ | | ● | | |
| ArkVale | NeurIPS'24 | ● | | | | ● | | |
| Cake | ICML'25 | ● | ● | | | ● | ● | |
| HCache | EuroSys'25 | ● | ● | | ○ | ● | ● | |
| Marconi | MLSys'25 | | | ● | | | ○ | |
| ShadowKV | ICML'25 | ● | | | ● | ● | ○ | |
| OrbitFlow | VLDB'26 | ● | | | | ● | | |
| KVServe | SIGCOMM'26 | ● | | | ● | | | ● |
| KVP | ICML'26 | | | ● | | | | |
| Het-Mem | IEEE CAL'25 | ● | | | | ● | | |
| MTDS | C&IS'26 | ● | ● | ○ | | ● | ● | |
| KVDrive | preprint | ● | ● | | | ● | | |
| EvicPress | preprint | ● | ● | ○ | ● | ● | | ○ |
| QEvict | preprint | | | | ● | ● | | |
| **Tiara（本文）** | — | ● | ● | ● | ● | ● | ● | ● |

（● = 明確支援，○ = 部分或間接支援。**判準從嚴：「未來預測」欄僅在該工作預測尚未發生的存取時才標記，
僅使用當前步 attention 分數者不計。**）

**三項觀察**：
1. **「重算」與「品質約束」兩欄從未在同一列同時標記。**
2. **四項成本從未同時進入單一線上決策**（最高覆蓋為 Marconi 的三項）。
3. **兩條研究血脈互不交會**：
   - 以 **FlexGen** 為基礎的一系（IMPRESS、LeoAM、InfiniGen）具備**重要性感知與有損處理，但無跨請求重用**；
   - 以 **vLLM** 為基礎的一系（CacheBlend、LMCache、Cake、MTDS）具備**跨請求重用，但將 KV 視為精確且不可分割**。

   → **尚無系統同時具備重要性感知的有損分層與跨請求重用。**

---

## 9. 論文尚未做到的事

> 論文第 7 章與附錄 B 自陳。**這一節不是缺陷清單，是「證據到哪裡為止」的界線。**

### 9.1 最主要的限制

**線上策略尚未實作，故本文不報告任何端到端效能。**
第 6 章以 oracle 量到了改善空間的**上界**，但取得該上界的策略**尚不存在**。
> **oracle 是上界不是成績。** 以 Sibyl 報告的「達成全知 oracle 的 80%」換算，
> 本文的上界對應生產 trace 上 **6.7–7.4%**、甜蜜點 **25–27%** 的實際改善——
> **此換算取自單一先前系統的達成率，不是本文的量測。**

### 9.2 其餘七項限制

| 限制 | 內容 |
|---|---|
| **$\epsilon$ 是估計值而非保證** | $\Delta\mathcal{Q}\le\epsilon$ 以 profiling 建立的代理訊號執行，是**經驗性估計而非形式保證**。這是所有品質約束工作的共同限制。 |
| **重算可能不划算** | 若 $p_i$ 的預測精度不足，`Drop` 可能持續造成淨損失。**若消融顯示 `Drop` 不帶來收益，論文將自系統中移除之。此檢驗尚未執行。** |
| **K 與 V 未分離處理** | 見 §8.8。拆為兩軸會使 $\lvert\mathcal{A}\rvert$ 自 6 增至 36。 |
| **ROCm 生態成熟度** | 平台 B 的移植風險**不在策略，而在其下的分層機制**：LMCache 無自動化 on-device ROCm CI、其預設 MP 模式有已知死鎖、vLLM 的 KV 傳輸錯誤會**導致 worker 終止而非降級**、啟用 KV connector 會使 vLLM **自候選清單移除 `ROCM_ATTN` 後端**。 |
| **與 AMD 生產堆疊的關係** | AMD 的 `ROCm/mori`（MORI-UMBP）已提供分層儲存與 SSD 層並整合入 SGLang HiCache。**論文不宣稱 AMD 平台缺乏分層 KV 基礎設施**；貢獻在**放置策略**而非分層機制。 |
| **平台 A 無能耗結論** | 消費級顯卡**缺乏記憶體與計算分軌的能耗計數器**。MI300X 的 `amdsmi` 有 `hbm_energy_acc`（HBM 與計算分離），**是做此分析的正確平台，但尚無該機器**。 |
| **階梯假設主機記憶體是獨立的一階** | 在 CPU/GPU 共用同一塊 HBM 的 **APU（如 MI300A）** 上，「卸載至 CPU」僅是頁表變更、**不產生容量收益**，階梯塌縮為 HBM → 對等 APU HBM → NVMe。**成本模型形式不變，但 `Cpu` 一階須重新標價。** |

### 9.3 無法重跑的對照組

| 類別 | 工作 | 原因 |
|---|---|---|
| **學習式** | KVP、ForesightKV、LookaheadKV | **均未釋出訓練後的權重**，且預測器**模型專屬**。重訓是「每個主模型各一次的完整訓練」。 |
| **稀疏檢索** | ArkVale、Quest、ShadowKV、ClusterKV、RetroInfer、ParisKV | **`rapidsai/raft` 無 ROCm 移植**（加上 CUTLASS 的 template 層級耦合）→ **結構性地無法於平台 B 執行**。可於平台 A 執行，**但無法提供這些方法在 MI300X 上的數值**。 |

> **論文的方法論回應**：核心主張是「**對稱損失在異質動作成本下失準**」，
> 而檢驗該主張的**正確方式是在同一系統內替換損失函數**（表 15 的 (B) 區塊），
> **此設計消除了系統間的無關差異**。直接比較 Tiara 與 KVP 並**不能**檢驗此主張
> ——二者在儲存層數（六階 vs 單階）、預測器族（GBDT vs RL）與特徵集上皆不同，
> **量到的差異無法歸因至損失函數**。外部學習式 baseline **僅用於佐證整體競爭力**。

### 9.4 尚未執行的實驗（附錄 B）

**表 15 的消融矩陣**是最重要的一項，分四個區塊：

| 區塊 | 回應什麼質疑 | 關鍵列 |
|---|---|---|
| **(A) 四項成本** | 「把四項成本組合起來是否構成貢獻」 | 逐項移除：未來效用 → recency、$\lambda_x=0$、移除 `Drop`、$\lambda_m=0$。**論文明確承認相反的可能：若移除某項不造成退化，正確做法是把它移除。** |
| **(B) 損失函數** | **核心主張的直接檢驗，不可省略** | 對稱 L2 @ 0.5 / **對稱 L2 @ $p^{*}$** / (L1) 加權+校準 / (L2) 成本進 NDCG |
| **(C) 機制與特徵** | 品質約束與精度維度確有作用 | 移除品質約束 / 移除精度維度 / 僅存取歷史 / 移除 `attn_mass` |
| **(D) 預測器容量與結構** | GBDT 的選擇是否站得住 | GBDT / MLP 2×64 / MLP 4×256（**容量**）/ **GRU（結構）**。**另行回報熱路徑推論延遲**。 |
| **(E) 精度的決策粒度 $g$** | 逐 block 決定精度是預設值，不是量測結論 | $g=16$（預設）/ **$g\approx128$（對齊 tile）** / $g=512$ / $g=\infty$（全域統一，即 §6.6 已量的錨點）。**另需 attention kernel 層級延遲**——tile 破碎會被端到端延遲吸收而無從歸因。 |

> **(E) 於平台 A 不具鑑別力**：§6.6 顯示三個低精度階中僅 INT8 保住品質，
> 且 `fp8_per_token_head` 所需的 Triton 後端於 sm_86 拒絕 FP8 KV
> → 平台 A 實際只有 **BF16 與 INT8 兩階**，兩階之下逐 block 與逐段的差異無從展開。
> CDNA3 具原生 FP8，故此區塊**須待平台 B**（`fp8_per_token_head` 在 ROCm 上是否可用為 `NOT_MEASURED`）。

> **最關鍵的可證偽點**：(B) 區塊**在兩平台上應呈現不同的退化幅度**
> ——在 $\kappa$ 較大的 3090 上，對稱損失的代價應更明顯。
> **若兩平台退化幅度相同，則硬體條件化的主張被否證。**

**平台 B（MI300X）的設計**：三軸壓力（上下文長度 128K/256K/512K/1M、模型架構、模型規模 8B–70B）。
選模準則是「**僅納入會使 $\kappa$ 產生可事先計算之變化的模型**」：

| 平台 | 模型 | $\kappa$ | 角色 |
|---|---|---|---|
| A (3090) | Qwen2.5-7B-Inst-1M | 108 | 主力（**懸崖在 315K，遠低於模型上限 → 量到的是純記憶體限制**） |
| A (3090) | Llama-3.1-8B-Inst | 54 | 對照（**$\kappa$ 減半**；其懸崖 ~132K 與原生上限 131,072 幾乎重合，兩種限制無法分離） |
| B (MI300X) | Llama-4-Scout (MoE) | 8 | 規模（原生 10M） |
| B (MI300X) | Qwen3-30B-A3B (MoE) | 3 | **MoE 對 $\kappa$ 的影響** |

**明確排除的架構**：MLA（改變式 (1) 本身）與 hybrid SSM（狀態不隨序列成長，已由 Marconi 處理）
——**二者並非「未評估」，而是定義上不同的問題**。

**尚未量測的指標**：TTFT、TPOT、端到端延遲、各階佔用、PCIe traffic、throughput、
服務層級分佈、重算成本、能耗、**真實長文本摘要在放置策略下的表現**
（已量的是 KV **精度**對摘要的影響；**放置策略已驗證為逐字元無損，故該欄空缺不影響本文主張**）。

> **ROCm 量測方法的一個坑**：KV 搬移經由 **SDMA copy engine 而非 AQL compute queue**，
> **對 `rocprof-compute` 的 TCC/L2 counter 不可見**。
> 須改用 `rocprofv3 --memory-copy-trace` 取得逐筆傳輸的位元組數、方向與時間戳。

---

## 附錄 A：公式速查索引

| # | 名稱 | 式子 | 在哪用 |
|---|---|---|---|
| (1) | KV footprint | $b_{\text{tok}} = 2 L H_{kv} d_h s$ | 所有容量計算；⚠️ 不含量化中繼資料 |
| (2) | **重算成本對位置線性** | $C_{\text{recompute}}(P) = 513.0 + 26.9\,P/1000$ ms | 式 (13) 的分母 $\alpha$；oracle 計價 |
| (3) | 系統狀態 | $s_t = (\{\ell_i,\pi_i,a_i,n_i\}, C^{\ast}, Q_{\text{pcie}}, \phi_t)$ | 問題形式化 |
| (4) | **動作空間** | $\mathcal{A} = \{\textsf{Gpu}_{16},\textsf{Gpu}_8,\textsf{Gpu}_4,\textsf{Cpu},\textsf{Ssd},\textsf{Drop}\}$ | 全文軸心 |
| (5) | `Drop` 可行性 | $a_i=\textsf{Drop} \iff \forall j<i:\ b_j$ 可取得 | 式 (8) 的約束 |
| (6) | 依賴鏈成本 | $\mathrm{recomp}(i)=\sum_{j\in\mathcal{D}(i)}(2N_{\text{active}}\lvert b_j\rvert + \mathrm{attn}(j))$ | 連續 `Drop` 的上限 |
| (7) | **成本模型（四項成本）** | $c(i,a)=\lambda_m\mathrm{mem}+\lambda_x\mathrm{xfer}+p_i[\mathrm{fetch}+\mathrm{recomp}]$ | 貢獻宣稱的核心 |
| (8) | **受約束最佳化** | $\min\sum c(i,a_i)$ s.t. $\Delta\mathcal{Q}\le\epsilon$, 容量, SLO, (5) | 問題定義 |
| (9) | **最佳門檻** | $p^{*}=c_{\text{FP}}/(c_{\text{FP}}+c_{\text{FN}})=1/(1+\kappa)$ | 演算法 1 第 10 行；損失論證的支點 |
| (10) | (L1) 加權 L2 | $\mathcal{L}=\sum_i w(a_i)(\hat y_i-\log\tau_i)^2$ | 表 15 (B) 區塊 |
| (11) | (L2) 成本進 NDCG | $\mathrm{DCG}=\sum_w \frac{\mathrm{cost}_{\text{err}}(a_w)(d(l_w)-1)}{\log(\mathrm{pos}(l_w)+1)}$ | 表 15 (B) 區塊 |
| (12) | decode 每步成本 | $t = \beta + \gamma N_{\text{blocks}}$ | go/no-go 條件一（權重須量化） |
| (13) | **成本交叉點** | $P^{*}=(C_{\text{ssd}}-C^0_{\text{recompute}})/\alpha$ | go/no-go 條件二（長度須落在 $2$–$3.5P^{*}$） |
| (14) | 品質純量化 | $Q(\pi)=\min_{t}r_t(\pi)$，約束 $Q(\pi)\ge 1-\epsilon$ | 品質閘；**取最差任務不取平均** |
| — | EDC 遞迴 | $C_i \leftarrow 1+C_i\cdot 2^{-\Delta_1/2^{9+i}}$ | 演算法 1 第 2 行 |
| — | 磁碟成本分解 | $C_{\text{ssd}}=C_{\text{lookup}}+C_{\text{sched}}+C_{\text{io}}(\text{device})$ | 只有末項與裝置有關 |

---

## 附錄 B：所有實測常數一覽

> **全部量測於平台 A（RTX 3090、sm_86、driver 550.163.01、CUDA 12.4、vLLM 0.28.0、PyTorch 2.13.0+cu129）。
> 平台 B 的常數為 `NOT_MEASURED`。**

### B.1 成本常數（ms/block，表 12）

| | `qwen-awq` NVMe **（主設定）** | `llama-bf16` NVMe | `llama-bf16` SATA |
|---|---|---|---|
| 量測 ctx | 96,000 | 16,384 | 16,384 |
| `Cpu` | 0.298 | 0.543 | 0.588 |
| `Ssd` | 10.245 | 6.286 | 5.536 |
| `Drop` 基底 $C^0$ | 3.546 | 4.008 | 4.008 |
| `Drop` 斜率 $\alpha$ | 0.000178 | 0.000210 | 0.000210 |
| **$P^{*}$（token）** | **37,717** | 10,851 | 7,278 |
| 位置擬合上限 | 258,048 | 24,576 | 24,576 |

**本文所有 oracle 與掃描結果的磁碟階皆為 NVMe；SATA 一欄僅供裝置對照，不用於任何結論。**

原始量測（`qwen-awq`/NVMe/96K 上下文，扣除 GPU 命中的 860.7 ms 基準）：
`CPU` 2,651.7 ms、`SSD` 62,328.9 ms；`Drop` 自位置 0 的 453.9 ms 成長至位置 258,048 的 6,320.2 ms。

### B.2 KV 精度階的容量（§6.9）

| KV dtype | KiB/tok | 理想 | 額外 | vs BF16 |
|---|---|---|---|---|
| `auto` (BF16) | 128.11 | 128 | +0.11 | 1.00× |
| `fp8` | 64.04 | 64 | +0.04 | **2.00×** |
| `int8_per_tok_head` | 66.04 | 64 | **+2.04** | 1.94× |
| `int4_per_tok_head` | 34.02 | 32 | **+2.02** | **3.77×** |

### B.3 decode 每步成本擬合（$R^2 = 0.999$）

| 剖面 | $\beta$ (ms) | $\gamma$ (ms/block) | 換算 KV 讀取頻寬 |
|---|---|---|---|
| `llama-bf16` | 36.768 | 0.005581 | 376 GB/s |
| `qwen-awq` | 18.158 | 0.001267 | 581 GB/s |

（RTX 3090 峰值 936 GB/s → 兩者皆低於峰值，**物理合理性檢查通過**）

### B.4 儲存裝置（§A.2）

| 裝置 | 裸讀 | 短測寫（1 GiB） | **持續寫（16 GiB）** | 端到端有效頻寬 |
|---|---|---|---|---|
| SATA QLC（Samsung 870 QVO） | 522 MiB/s | 492 MiB/s | **181 MiB/s（37%）** | 353 MiB/s（裸讀的 68%） |
| NVMe（Crucial P3） | 2,085 MiB/s | 2,512 MiB/s | **`NOT_MEASURED`** | 311 MiB/s（裸讀的 **15%**） |

**裸讀差 4.0×，端到端有效頻寬只差 1.1× → 於 NVMe 上，磁碟階的成本有 85% 不是 I/O。**

### B.5 $\kappa$ 與硬體比值

| 平台 | Mem BW | Host link | Mem:link | FLOP/byte | $\kappa$ (8B / 70B) |
|---|---|---|---|---|---|
| RTX 3090 | 0.94 TB/s | 31.5 GB/s | 30:1 | 2,254 | **54 / 190** |
| H100 SXM | 3.35 TB/s | 64.0 GB/s | 52:1 | 15,459 | — |
| H200 SXM | 4.8 TB/s | 64.0 GB/s | 75:1 | 15,459 | — |
| MI300X | 5.3 TB/s | 63.0 GB/s | 84:1 | 20,752 | **6 / 21** |
| MI355X | 8.0 TB/s | 63.0 GB/s | 127:1 | — | — |
| GH200 | ~4 TB/s | 450 GB/s | ~9:1 | — | — |

**$\kappa$ 跨平台變動 32 倍（190/6）。FLOP/byte 跨本文兩平台相差 9.2 倍。**

---

## 讀完之後

- **論文主張** → `main.tex`
- **實驗怎麼跑** → `EXPERIMENT_PLAN.md`
- **在這台機器上怎麼動手** → `CLAUDE.md`
- **已知但刻意暫緩的問題** → `OPEN_ISSUES.md`
- **原始量測** → `results/m4_oracle/`、`results/RUNLOG.md`

**一句話收束**：
> 當動作成本相差 6–190 倍、且該比值本身隨硬體變動 32 倍時，
> **以對稱損失訓練的效用預測器會系統性地錯置決策邊界**。
> 而本文最誠實的一句是：**線上策略尚未實作，oracle 是上界不是成績。**
