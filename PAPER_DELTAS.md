# 待更新的論文主張（2026-08-31 量測之後）

> 這份檔案是**暫存區**，不是論文。每一列都是「main.tex 現在怎麼寫」→
> 「量測說什麼」→「證據在哪」。改完 main.tex 之後這一列就刪掉。
>
> 規則：沒有 `results/` 裡的檔案可以指的，不准寫進論文（CLAUDE.md 禁令 1、3）。

## A. 必須修正（現在的寫法已被實測推翻）

### A1 ✅ 已改進 main.tex（§app:oracle 新增「逐 block 的簡化對前綴結構的工作負載無害」段落）

---

### A2 ✅ 已改進 main.tex（§app:oracle 驗證段改寫，限制放寬並標明範圍）

---

### A3 ✅ 已改進 main.tex（§app:cost 磁碟段更新 + 新增 QLC 持續寫入段）

---

## B. 新增（今天量到、論文還沒有的）

### B1. 寫入頻寬把問題從「延遲」變成「可行性」★

模擬的成本模型只向「讀回來」收費，**寫下去是免費的**。實際上：

| SSD 容量 | 最佳 baseline | headroom | baseline 寫入需求 | Oracle 寫入需求 |
|---|---|---|---|---|
| 0 GiB | cpu_arc | 8.23% | 0 | 0 |
| 32 GiB | cpu_arc | 8.44% | 0 | 267 MiB/s |
| 128 GiB | tier_fs | 8.44% | **4,666 MiB/s** | 617 MiB/s |
| 512 GiB | tier_fs | 2.75% | **4,666 MiB/s** | 658 MiB/s |

`/ssd7` 的持續寫入能力是 **181 MiB/s**。也就是說 `tier_fs` 一旦贏過 `cpu_arc`，
它要求的寫入頻寬是裝置能力的 **25.8 倍——在真機上跑不起來**。
Oracle 只要 1/7.6 的寫入量，在 NVMe（2,512 MiB/s）上可行。

寫入次數的下界（每個不重複 block 只寫一次）是 3,086 MiB/s，仍超出 17 倍，
所以這個結論不依賴「load-and-evict vs load-and-keep」的建模選擇。

**這是論文主軸該換的地方**：原本賣「省 8.4% 延遲」（MARGINAL），
實際的差別是「這個階層到底能不能用」。

**證據**：`results/m4_oracle/ssd_sweep.csv`、`results/m2_harness/disk_bw*.csv`

### B2. κ 在同一台機器的兩顆碟之間就差 13.9 倍

181（SATA QLC 持續寫）vs 2,512（NVMe）MiB/s。
論文的 κ 跨硬體主張**不需要等平台 B 就有第一個實證**。

### B3. Oracle 的重算次數等於強制未命中下限（模擬器自我驗證）

`cascade` 模式下，Oracle 在每一個預算設定下的重算次數都**恰好等於不重複 block 數**
（toolagent 183,300、conversation 182,790，粒度修正前的值）——
即每個 block 只算一次，一次都不重複。這是模擬器正確性的獨立驗證。

`cost-aware` 模式下會**高於**下限，因為 Oracle 會在「重算比存放便宜」時
主動丟棄——那正是論文 DROP 動作被啟用的徵兆。

### B4. headroom 在高壓力下飽和

跨四個數量級的壓力（60× → 16,600×），headroom 只在 13.5%–16.3% 之間變動。
「壓力越大 headroom 越大」**不成立**，它有上界，上界由強制未命中決定。
（此表為粒度修正前的值，修正後重跑中。）

---

### B5. 預測器的品質不是瓶頸，**排序**才是（M5 第二階段，2026-09-05）★

論文 §5.2 把預測目標定為 $\log\tau$ 的迴歸，§B.6 刻意不報分類準確度，改報
**校準品質**與**成本加權錯誤**。實測顯示這三個量**都測不到**真正決定策略成敗的東西：

| trace | AUC | ECE | Spearman（正樣本內） | 線上策略拿到的 headroom |
|---|---|---|---|---|
| toolagent | 0.9995 | 0.0003 | **0.233** | 10.0% |
| conversation | 0.9988 | 0.0006 | 0.999 | 4.2% |

把**逐出順序**換成真值、其餘（目的地規則、成本模型、記帳）完全不動：
toolagent 22.79M → **20.60M ms**、conversation 24.33M → **23.03M ms**。
也就是說線上策略與 oracle 的差距**幾乎全部來自排序**，不是來自成本模型或門檻。

機制有兩層，第二層是設計層級的：
1. AUC 問的是「會不會在 $W$ 內被用到」，Bélády 要的是「誰先被用到」；
2. 更關鍵：標籤在 $W$ 處**設限**，於是所有「$W$ 之外」的 block 預測值全部擠在
   $\log(W{+}1)$ 附近成為**同分**——而它們佔 conversation 的 95.7%。
   真值能分辨「$2W$ 後會用到」與「再也不會用到」，設限後的迴歸目標不能。

**建議**：(i) §B.6 的指標清單加一項「下次使用時刻的排序品質」；
(ii) §5.3 的 (L2)（排序損失）比 (L1)（加權 L2）更值得做，但理由不是論文現在寫的
「成本要進 gain 項」，而是「策略吃的就是排序」；
(iii) 演算法 1 的門檻用 $\hat p$ 沒問題，但逐出順序不該用同一個設限過的分數。

**證據**：`results/m5_predictor/predictor_metrics.csv`（`spearman_positives`）、
`policy_sim.csv`（`diag_true_ordering` 列）

### B6. Elkan 基線吃掉全部增益（OPEN_ISSUES B4 的答案是「會」）

§5.3 對 (B) 區塊給了一個可證偽的預測：**加權訓練與「對稱訓練 + 事後移門檻」的
差距應隨 $\kappa$ 增大而擴大**。在預測夠難的設定（$W=35\times$、測試段正樣本率 50.4%、
AUC 0.896）上量到的是**相反方向**：

| 位置分箱 | $\kappa$ | 對稱@$p^*$ | 加權@$p^*$ | 加權的增益 |
|---|---|---|---|---|
| 0–4K | 12.0 | 33,029 ms | 33,719 ms | −2.1% |
| 16–32K | 24.1 | 22,684 ms | 25,863 ms | −14.0% |
| 32–64K | 37.2 | 13,060 ms | 24,069 ms | −84.3% |
| 64–128K | 60.0 | 3,546 ms | 18,742 ms | **−428.5%** |

而**門檻移動本身**（Elkan 基線）值 **80%**：對稱@0.5 = 667,207 ms → 對稱@$p^*$ = 132,291 ms。
加權訓練在其上是 **−25%**（165,298 ms）。

依 §5.3 自己寫的判準——「若本文的增益全部來自門檻移動，則加權訓練並無貢獻」——
**這一輪的結論是主張應縮減至門檻校準**。$n=1$ 個 seed、單一 trace、單一平台，
但方向與幅度都不像雜訊（高 $\kappa$ 區差 4 倍以上）。

**可能的機制（尚未驗證）**：權重 $w=C_{\text{drop}}(pos)/C_{\text{cpu}}$ 把擬合能力
推向長位置樣本，而 isotonic 校準是**全域**擬合的——加權造成的偏移在高 $\kappa$ 區
變成校準誤差，正好落在最貴的地方。可測試的下一步：逐位置分箱校準。

**證據**：`results/m5_predictor/cost_by_position.csv`、`predictor_metrics.csv`

### B7. 🔴 M4 的 oracle 少算了 headroom：修好之後 8.4% → 12.3%（2026-09-05 已重量）

**症狀**（第一輪發現）：同一段 trace、同樣的記帳，只把 SSD 階由 0 開到 512 GiB，
`run_oracle` 反而**慢 3.3%**。最佳策略不可能因為多了一個可用資源而變差。

**原因**：`cost-aware` 的目的地規則拿「整條尾巴」（`tail_of`）當丟棄的邊際成本。
前綴語意下缺一塊會使其後全部重算，所以尾巴是**上界**——但那條尾巴是該請求
所有缺塊**共用**的，整條算給每一個 block 等於重複計價，門檻因而被壓得太低。
後果：把 25 萬個 block 寫上 SSD、再用 10.245 ms 讀回，而重算只要 3.5–4 ms
（$P^{*}=37{,}635$ token 以下重算較便宜，而幾乎所有重用都落在 8K 以下）。

**修法**：新增 `dest="marginal"`（丟棄成本只算 block 自己那一塊），
`dest="best"` 改取 cascade／cost-aware／marginal 三者較佳。
oracle 的定義是「我們能構造出的最佳**離線**策略」，多一條可實作的規則只會更緊。
**`sim_version` 因此由 `b0d39dd4` 變為 `fbea1a98`。**

**重量結果**（qwen-awq／NVMe／完整 trace／端到端含 decode）：

| trace | SSD | 論文現值（舊 oracle） | 修正後 | 差 |
|---|---|---|---|---|
| toolagent | 512 GiB | 8.42% | **12.33%** | +46% |
| conversation | 512 GiB | 9.23% | **12.00%** | +30% |
| toolagent | 0 GiB | — | 12.29% | |
| conversation | 0 GiB | — | 11.97% | |

**容量單調性已恢復**：0 → 512 GiB 的 headroom 由 12.29 → 12.33%（toolagent）、
11.97 → 12.00%（conversation），方向正確。
oracle 的 SSD 寫入也由「大量」降到 137,136 次（34 MiB/s）／175,925 次（44 MiB/s），
遠低於裝置的 2,512 MiB/s，**可行性判定不再是問題**。

**新增的自動守門**：`m4_invariants.check_capacity_monotone`——同一設定下加大任一階容量，
oracle 的 `total_ms` 不得上升。`m4_sweep.py --axis ssd` 每條 trace 掃完會跑一次。

**論文要改的地方**：§6.10 的 go/no-go 表（8.42%／9.23%）與所有引用該值之處。
**注意這不改變判定**——12.3% 仍落在 5–15% 的 MARGINAL 區間，
但「條件式 GO」的兩個條件（權重須量化、長度須落在甜蜜點）論證更強了。
其餘帶舊 `sim_version` 的 CSV（`ssd_sweep`、`budget_sweep`、`headroom_surface`…）
**都需要重跑**才能與新值並列。

**證據**：`results/m4_oracle/qwen-awq-oraclefix/ssd_sweep.csv`

### B8. 文獻補件：兩篇直接影響 §5.3 主張的既有工作（2026-09-05 檢索）

⚠️ **以下皆為檢索所得的摘要層級資訊，尚未逐篇讀全文。** 依 `citation-audit` 的規矩，
寫進論文前必須確認「引用語境是否被原文支持」。

**(1) Zhao, H. 《Instance weighting versus threshold adjusting for cost-sensitive
classification》, Knowledge and Information Systems 15:321–334 (2008),
doi 10.1007/s10115-007-0079-1。**

這篇正面比較了本文 §5.3 的兩個選項——**樣本加權**（(L1)）與**事後移門檻**（Elkan 基線）。
其結論是：對 naive Bayes／logistic regression／類神經網路，兩者結果**相近**；
對**決策樹、決策表、規則學習器**則**差很多**。

**為什麼這對本文是關鍵**：Tiara 用的是 GBDT，正是樹族。§5.3 目前把「加權訓練會贏」
寫成一個由「有限容量模型的擬合能力配置」推導出的預期，但這個問題在成本敏感學習
文獻裡**已經被實證比較過**，而且結論是「視學習器而定、沒有一致的贏家」。
本文 B6 量到的（加權在高 κ 區輸給 Elkan 基線 4 倍以上）與這條線索一致。

**建議**：§5.3 引用此文，把主張改成「哪一個較好取決於模型族與資料，本文對 GBDT
在此工作負載上量到的是門檻移動勝出」，而不是先驗地預期加權會贏。

**(2) LRB 對「殘差來自預測誤差」的說法可以再分解。** LRB 的敘述是
「LRB 與 relaxed Belady 之間的差距來自模型的預測誤差」，並提出 **good decision ratio**
當診斷。本文 B5 把那個「預測誤差」拆成兩塊，並顯示**分類品質幾乎完美而排序品質很差**
（AUC 0.9995 / Spearman 0.233）。檢索沒有找到既有工作報告這個分解，
它應該是本文可以主張的一個小貢獻——但**必須先把 good decision ratio 一起量出來**，
否則審稿人會問「你這個 Spearman 與 LRB 的 good decision ratio 有何不同」。

**(3) 兩篇需要追的近作**（尚未讀，僅記位置）：

| 論文 | 為什麼要追 |
|---|---|
| CacheWise（arXiv 2606.16824）：LLM coding agent 的 KVCache 管理 | 同樣做 prefix-aware 排程 + reuse-aware 逐出，但以**工具呼叫的中繼資料**做輕量預測、**沒有**重算 vs 傳輸的成本模型。是相關工作，也可能是「長上下文 + 真實重用」trace 的來源（本文最實質的證據缺口） |
| MTDS（Complex & Intelligent Systems, doi 10.1007/s40747-025-02200-4） | 多階 KV 儲存，明文「依**預測的未來命中機率**調整儲存優先序」。這是目前找到最接近本文核心的既有工作，**必須讀**。付費牆，本次沒讀到 |

另外 **LHD（NSDI'18，hit density）** 不在 `refs.bib` 裡，但它正是「以經濟量（每單位資源的期望命中）
排序而非以分類排序」的先例，與 B5 的建議同一條線，值得補進 §5.2。

---

### B9. 在論文自己的高 headroom 設定上，線上策略拿到 82% 的空間——而且全部來自「排序」

第一輪在 Mooncake／端到端上量，那裡 oracle headroom 只有 8.5%。
改用論文 §6.8 的 `surface:L131072:rq10`（重用 89.06%、壓力 6.41×、prefill-only）之後：

| 設定 | oracle headroom | 線上策略 | 拿到 |
|---|---|---|---|
| 長上下文 131K，prefill | 18.81% | **+15.40%** | **81.85%** |
| 長上下文 131K，端到端 | 16.09% | +13.17% | 81.85% |
| Mooncake toolagent，端到端 | 11.50% | +0.76% | 6.62% |

**拆解（把逐出順序退回 LRU、目的地規則不變）**：長上下文設定上 LRU 排序 + 成本模型
= **+0.00%**（逐位元等於 `tier_fs`），預測排序 + 成本模型 = **+15.40%**。
**是排序在賺。** Mooncake 上相反：把目的地規則換成 cascade 就掉到 −7.79%，門檻才是賺的那一半。
**兩個元件在不同區間才有價值——論文不能只講其中一個，也不能把兩者的增益相加。**

⚠️ 三個必須跟著寫的限制：
1. **冷啟動**：`--segment test` 的評估中快取是冷的但**特徵狀態是熱的**；
   兩者都冷（`--segment full`）時同一支策略是 **−16.13%**。
2. 那個設定只有 **120 個獨立事件**（見 B11），所以 82% **不能宣稱是「學習」的功勞**——
   訓練出來的模型 AUC 恰為 0.500，只是它的 $\hat\tau$ 仍帶弱排序（Spearman 0.13）。
3. 預測失效時同一機制會反噬（見 B10）。

**證據**：`results/m5_predictor/policy_sim.csv`（`ordering` 欄 = `lru` vs `predicted`）

### B10. 🔴 預測沒有訊息時，排序式策略比 LRU 差 2.7–5.6 倍

`lc128ks` 測試段上 oracle 與最佳 baseline 完全相同（headroom 0.00%），而線上策略是
**4,476,442 ms vs 1,649,587 ms（−171%）**；`lc32ks` 上是 −458%。
**重算率完全一樣**（強制未命中），差別全在「從哪一階取回」：GPU 命中率由 49.0% 掉到 14.1%，
40.9% 的存取被推到 SSD（10.245 ms/block）。

機制：$\hat p$ 中位數 0.768 且 100% 的 block 都 > 0.5 → $\hat\tau$ 近乎常數 →
**逐出順序等於隨機**。把排序換回 LRU 只剩 −3.49%。

**LRU 的 recency 排序本身就是一個很強的先驗，而排序式策略在分數打平時把它丟掉了。**
論文應寫入：預測器需要一個**「我不知道」的退路**（分數離散度低於門檻就回退 LRU），
否則在預測失效的工作負載上是數倍的損失，而不是「跟 baseline 差不多」。

**證據**：`results/m5_predictor/policy_sim.csv`（`lc128ks`／`lc32ks` 的列）

### B11. §6.8 的 headroom 曲線是 120 個獨立事件掃出來的

`surface:L131072:rq10` = 12 份文件、120 個請求。一份 131K 文件是**一個事件**，
其 8,192 個 block 的特徵與標籤幾乎相同——「985,483 個樣本」只帶 120 個事件的資訊。

| 工作負載 | 獨立事件 | 每事件重複 | 訓練後 AUC |
|---|---|---|---|
| lc128kz（§6.8 設定） | **120** | 8,355 block | **0.500** |
| Mooncake toolagent | 23,608 | 538 | 0.9995 |

**那條曲線可以支持「oracle 有多少空間」，但不能支持任何關於學習式方法的主張。**
論文若要在長上下文區間宣稱學習有效，需要獨立事件數量級更大的工作負載。

### B12. 特徵：LRB 的 deltas/EDC 在 KV 上沒有貢獻，static 才是訊號

同一設定內（toolagent、W=1×、取樣 0.25、葉 63）：

| 特徵族 | AUC | Spearman | 成本加權錯誤 |
|---|---|---|---|
| **只有 static**（位置、請求長度、位置差、請求間隔） | **0.9998** | 0.229 | **579.84 ms** |
| 只有 deltas | 0.9997 | 0.147 | 753.68 ms |
| 只有 EDC | 0.9997 | 0.113 | 762.64 ms |
| history+deltas+EDC（無 static） | 0.9995 | 0.122 | 824.23 ms |
| 全部 | 0.9995 | 0.233 | 829.00 ms |

**只用 static 比用全部好 30%。** §5.2 的特徵設計整個建立在 LRB 的 deltas+EDC 上，
但 **KV block 的重用是請求結構驅動的，不是 CDN 那種物件熱度驅動的**。
（⚠️ 此處沒有 pooled KV 與 attn_mass 兩族，所以這**不是**表 15 (C) 區塊的完整結論。）

### B13. 跨工作負載：排序會轉移，校準不會——所以重訓的是校準器不是模型

| 訓練於 → 測試於 | AUC | ECE | 成本加權錯誤 |
|---|---|---|---|
| conversation → conversation | 0.9988 | 0.0006 | 350 ms |
| conversation → **toolagent** | 0.9777 | **0.2874** | **3,385,691 ms** |
| toolagent → toolagent | 0.9995 | 0.0003 | 829 ms |
| toolagent → conversation | 0.9941 | 0.0006 | 5,889 ms |

**AUC 幾乎不掉而 ECE 惡化 1,000 倍、成本惡化 4,000 倍**——門檻施加在校準後的機率上，
校準一崩決策就全錯。

**這是 §5.2 第 (ii)(iii) 層的具體答案，而且是正面的**：跨工作負載時
**不必重訓 GBDT，只要重擬 isotonic 校準**（毫秒級、只要最近一小段樣本），
比 KVP 以「日」計的重訓便宜數個數量級。

**證據**：`results/m5_predictor/cross_workload.csv`

---

## C. 尚未量到、論文不得宣稱的

| 項目 | 為什麼還不能寫 | 排程 |
|---|---|---|
| GPU-FP8 / GPU-INT4 的反量化成本 | M2 只量了 4 階，六階動作空間缺兩階 | 今晚 03:00 |
| AWQ 剖面的成本模型 | 目前只有 llama-bf16 自洽；AWQ 是論文主設定 | 今晚 03:00 |
| 重算成本在位置 > 24,576 的行為 | 5.1 倍外插，12.6–20.5% 的存取落在外插區 | 今晚 03:00（掃到 114,688） |
| 品質 ε(f) 的混合精度曲線 | n=1000 的四個 dtype 跑完才排得上 | 今晚 |
| 平台 B（MI300X）的 κ | 沒有機器 | 待借卡 |
| 多租戶 / 機會成本 | 24 GB 放不下兩個 128K session | 平台 B 才能做 |
