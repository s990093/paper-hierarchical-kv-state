# 待更新的論文主張（2026-08-31 量測之後）

> 這份檔案是**暫存區**，不是論文。每一列都是「main.tex 現在怎麼寫」→
> 「量測說什麼」→「證據在哪」。改完 main.tex 之後這一列就刪掉。
>
> 規則：沒有 `results/` 裡的檔案可以指的，不准寫進論文（CLAUDE.md 禁令 1、3）。

## A. 必須修正（現在的寫法已被實測推翻）

### A1. §app:oracle —— 「前綴語意修正後 headroom 只會上升」

**現在寫**：
> 已知的偏差來源為前綴語意：vLLM 的 prefix cache 於中間缺塊時須重算其後全部，
> 而模擬器記為重算單一 block，因而低估 `full_gpu` 的成本。**修正此項後 headroom 只會上升。**

**量測說**：修正了，headroom 變化 **≤ 0.22 個百分點**（多數為 0.00）。
原因是結構性的，不是量級小：真實 LLM 流量的重用**本身就是前綴結構的**
（Mooncake 的 `hash_ids` 就是前綴雜湊），所以第一個未命中剛好落在共用前綴的結尾，
其後 87–100% 是這輩子第一次出現的 block，本來就要重算——
「缺口之後還有東西可以損失」這件事幾乎不發生。

| trace / 策略 | 缺口後 block | 其中仍在某一階 |
|---|---|---|
| toolagent / full_gpu | 180,699 | **144** |
| conversation / tier_fs | 170,877 | **0** |

**證據**：`results/m4_oracle/semantics_ablation.csv`；RUNLOG「發現 B」

**改法**：從「已知偏差、修了會更好」改成「已檢驗、對前綴結構的工作負載無害」——
這反而是模擬器的一項**有效性證據**，該放在正面而不是限制。

---

### A2. §app:oracle —— 「量級高估約 1.5 倍，故只引用趨勢不引用絕對值」

**現在寫**：
> 方向一致（2/2），但量級高估約 $1.5\times$（實測 9.00 對模擬 14.33）。
> **故本文僅引用 headroom 隨工作負載偏斜的趨勢，不引用其絕對值。**

**量測說**：

| ctx | 實測比值 | 模擬比值 | 差 |
|---|---|---|---|
| 16,384 | 9.00 | 9.73 | **8%** |
| 32,768 | 12.27 | 12.66 | **3%** |

絕對值也相符，不只是比值：
`full_gpu` 5,033.2 → 5,863.7 ms（+16%）、`cpu_lru` 559.4 → 602.5 ms（+8%）。

14.33 那個數字產生於 **SSD 成本常數修正之前**（當時量到的 0.40 ms/block 其實是
CPU 階，真值 5.54，差 13.7 倍）。

**證據**：`results/m4_oracle/simulator_validation.json`

**改法**：限制可以放寬，但要如實標明範圍——只在**一種工作負載形狀、兩個有鑑別力的
context** 上驗證過。不能寫成「模擬器已充分驗證」。

---

### A3. §app:cost —— 磁碟頻寬的數字

**現在寫**：裝置頻寬分別為 0.49 與 1.65 GB/s（相差 $3.4\times$）

**量測說**（`code/disk_bw.py`，O_DIRECT 循序，3 次中位，整機 HEAVY）：

| 裝置 | 1 GiB 測試 | 16 GiB 測試 |
|---|---|---|
| Samsung 870 QVO（SATA QLC，`/ssd7`） | 寫 492、讀 522 MiB/s | 寫 **181**、讀 515 MiB/s |
| Crucial P3（NVMe，`/`） | 寫 2,512、讀 2,085 MiB/s | — |

**QLC 的寫入懸崖**：短測落在 SLC 快取裡，樂觀 **2.7 倍**。
KV 階是持續寫入，所以 181 才是該引的值。

論文的 0.49 GB/s 與短測的 492 MiB/s 相符；1.65 GB/s 與我量到的 2,085 MiB/s 讀
不符，需要確認原始量法（可能沒有繞過 page cache，或用了不同的區塊大小）。

**證據**：`results/m2_harness/disk_bw.csv`、`disk_bw_sustained.csv`

---

## B. 新增（今天量到、論文還沒有的）

### B1. 寫入頻寬把問題從「延遲」變成「可行性」★

模擬的成本模型只向「讀回來」收費，**寫下去是免費的**。實際上：

| SSD 容量 | 最佳 baseline | headroom | baseline 寫入需求 | Oracle 寫入需求 |
|---|---|---|---|---|
| 0 GiB | cpu_arc | 8.23% | 0 | 0 |
| 32 GiB | cpu_arc | 8.44% | 0 | 267 MiB/s |
| 128 GiB | tier_fs | 8.44% | **4,666 MiB/s** | 617 MiB/s |
| 512 GiB | tier_fs | 2.75% | **4,666 MiB/s** | 658 MiB/s |

`/ssd7` 的持續寫入能力是 **181 MiB/s**。也就是說 `tier_fs` 一旦贏過 `cpu_arc`，
它要求的寫入頻寬是裝置能力的 **25.8 倍——在真機上跑不起來**。
Oracle 只要 1/7.6 的寫入量，在 NVMe（2,512 MiB/s）上可行。

寫入次數的下界（每個不重複 block 只寫一次）是 3,086 MiB/s，仍超出 17 倍，
所以這個結論不依賴「load-and-evict vs load-and-keep」的建模選擇。

**這是論文主軸該換的地方**：原本賣「省 8.4% 延遲」（MARGINAL），
實際的差別是「這個階層到底能不能用」。

**證據**：`results/m4_oracle/ssd_sweep.csv`、`results/m2_harness/disk_bw*.csv`

### B2. κ 在同一台機器的兩顆碟之間就差 13.9 倍

181（SATA QLC 持續寫）vs 2,512（NVMe）MiB/s。
論文的 κ 跨硬體主張**不需要等平台 B 就有第一個實證**。

### B3. Oracle 的重算次數等於強制未命中下限（模擬器自我驗證）

`cascade` 模式下，Oracle 在每一個預算設定下的重算次數都**恰好等於不重複 block 數**
（toolagent 183,300、conversation 182,790，粒度修正前的值）——
即每個 block 只算一次，一次都不重複。這是模擬器正確性的獨立驗證。

`cost-aware` 模式下會**高於**下限，因為 Oracle 會在「重算比存放便宜」時
主動丟棄——那正是論文 DROP 動作被啟用的徵兆。

### B4. headroom 在高壓力下飽和

跨四個數量級的壓力（60× → 16,600×），headroom 只在 13.5%–16.3% 之間變動。
「壓力越大 headroom 越大」**不成立**，它有上界，上界由強制未命中決定。
（此表為粒度修正前的值，修正後重跑中。）

---

## C. 尚未量到、論文不得宣稱的

| 項目 | 為什麼還不能寫 | 排程 |
|---|---|---|
| GPU-FP8 / GPU-INT4 的反量化成本 | M2 只量了 4 階，六階動作空間缺兩階 | 今晚 03:00 |
| AWQ 剖面的成本模型 | 目前只有 llama-bf16 自洽；AWQ 是論文主設定 | 今晚 03:00 |
| 重算成本在位置 > 24,576 的行為 | 5.1 倍外插，12.6–20.5% 的存取落在外插區 | 今晚 03:00（掃到 114,688） |
| 品質 ε(f) 的混合精度曲線 | n=1000 的四個 dtype 跑完才排得上 | 今晚 |
| 平台 B（MI300X）的 κ | 沒有機器 | 待借卡 |
| 多租戶 / 機會成本 | 24 GB 放不下兩個 128K session | 平台 B 才能做 |
